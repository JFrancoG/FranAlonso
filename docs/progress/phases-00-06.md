# Historical Progress — Phases 00–06 and Phase 07 preflight

> Archivo histórico preservado durante la optimización de gobernanza del 11 de agosto de 2026. El estado operativo
> actual vive en [`../Progress.md`](../Progress.md); la evidencia nueva de fase 07 vive en [`phase-07.md`](phase-07.md).

Last updated: 2026-08-10

## Current Snapshot

- Current gate: phase 06 is complete and integrated into `main` through PR [#2](https://github.com/JFrancoG/FranAlonso/pull/2), whose rebase result ends at `c368829`. The audited 06.7 functional tip is `44abac1`; its security-race, stale-evidence, explicit-`Sendable`, preview-coverage and cancellation findings are all closed, with both focused re-audits reporting no remaining issue. The separate formatting delivery is also integrated through `3f58314`. Phase 07 preparation is the next source-backed gate: its proposal, independent read-only review and explicit owner approval remain required before executable work. Every SyncEngine remains inactive.
- Overall status: phases 01–06 are complete and definitively delivered. Develop and Production retain distinct schemes, configurations, bundle identifiers and Firebase app registrations within the single approved Firebase project; provisional Rules keep all new collection namespaces closed. Phase 06 composes Firebase Auth, the email/password login, restored-session biometrics and local Keychain/SwiftData principal authorization behind one `AuthenticationRootViewModel`. The protected shell requires the stream principal plus current local evidence; email/password remains a recovery path when biometrics is unavailable. No live Firebase account, AutoFill flow, Keychain item, SwiftData production store or system biometric prompt was exercised.
- Product design status: Ciruela premium is the selected brand palette; navigation uses a simple adaptive tab shell with Jornada as the initial operational screen and Histórico as a separate section. ADR 0010 adds a visible, foreground-only, on-device voice session to the MVP: Foundation Models may read, navigate, and fill reversible drafts, while Fran keeps visual control of every save. GPT-5.6 Luna is prepared for post-MVP evaluation and GPT Realtime remains an unplanned option.
- Validation status: Xcode MCP lists 609 enabled Swift Testing declarations and the complete `FranAlonso-Develop` plan passes 780/780 results. The final app build succeeds in 6.870 seconds with no structured error or warning and the Issue Navigator reports 0 entries. Two audit regressions first failed 0/2 because consecutive `nil → same UID` and `other UID → original UID` events reopened the prior shell; the stream-owned local-access revision makes both pass 2/2. A third cancellation regression failed 0/1 because recent credential proof survived a cancelled authorization and now passes 1/1. The complete root suite passes 13/13. `AuthenticationRootScreen`, `LoginScreen`, `SessionScreen` and `ContentView` render without errors or clipping in Large, XXX Large and AX 5; the new denied, observation-failed and authenticated root previews also pass those three variants. Catalog JSON, layer-import, forbidden-concurrency/API, line-width and whitespace checks are clean. Both focused specialist re-audits report no findings. The provisional Firestore Rules retain their prior 23/23 evidence.
- Repository status: Git is initialized with the private GitHub `origin` configured over SSH. Phases 03 and 04 are integrated into `main` through `d0fca87` and `4b74266`; the complete audited phase 05 history is integrated through `20a4dde`. Phase 06.1–06.7 were rebased into `main` by PR [#2](https://github.com/JFrancoG/FranAlonso/pull/2): the integrated sequence runs from `f6c0db7` through `c368829`, with 06.7 functional code in `44abac1`. The former phase branch had an identical tree and all sixteen patches matched upstream before its local and remote references were deleted. PR [#3](https://github.com/JFrancoG/FranAlonso/pull/3) subsequently integrated the separate source-formatting delivery through `3f58314`; local `main` and `origin/main` are identical at that commit, the worktree was clean before this progress update, and no feature or formatting branch remains locally or remotely.
- Governance status: accepted ADR 0011 replaces the duplicate full-diff audit loop with an architecture/data/concurrency reviewer plus a conditional SwiftUI/accessibility reviewer. Governance requires native Swift construction instead of mechanically translated ceremonial patterns, a demonstrated responsibility or boundary for every abstraction, and only the most specific protocol in a conformance list. Concrete deterministic Data/Domain conversions without dependencies now live on extensions of the Data-owned DTO or persistent model; `toDomain()` reconstructs Domain and an unlabeled initializer performs the inverse, while a `Mapper` is reserved for owned dependencies, configuration, versioning, policy or interchangeable strategies. Domain never knows Data representations. Project-owned case-less enums may not serve merely as static namespaces: the semantic replacement is an extension on the owner type, a real service or value, or a free function or constant with the narrowest workable access; a genuinely uninhabited contract remains a documented exception, and an empty struct is not a mechanical substitute. The remaining SwiftUI, context and preview gates stay unchanged.

## Phase Overview

| Phase | Status | Evidence / next action |
|---|---|---|
| 00 — Governance | Complete | `AGENTS.md`, guide, checklist, progress tracker, specs, and ADR drafts installed. |
| 01 — Constitution | Complete | ADR 0002–0010 remain accepted; accepted ADR 0011 supersedes ADR 0001 and defines the current SwiftUI/review boundary. |
| 02 — Bootstrap | Complete | Subphases 02.1–02.10 are complete; the final governance verification and both independent audits are clean. |
| 03 — Architecture | Complete | 03.1–03.6 complete; Store criteria and boundary/naming review checks are validated with no open findings. |
| 04 — Domain | Complete | 04.1–04.8 are complete; proposal review and both final audit passes are closed with no open findings. |
| 05 — Data and sync | Complete | 05.1–05.11 are complete, audited and integrated into `main` through `20a4dde`. |
| 06 — Authentication | Complete | 06.1–06.7 are audited and integrated into `main` through `c368829` by PR #2. |
| 07 — Design/navigation | Not started | Architecture, bootstrap, Domain, Data/sync and authentication foundations are complete; pending the source-backed phase proposal, independent review and explicit owner approval. |
| 08 — Clients/consent | Not started | ADR 0009 accepted; pending domain, data/sync, authentication, and design-system foundations. |
| 09 — Products/stock | Not started | Pending domain/data foundations. |
| 10 — Services | Not started | Pending products and domain/data foundations. |
| 11 — Sales | Not started | Pending clients and services. |
| 12 — Sale/stock | Not started | Pending sales and stock. |
| 13 — Billing | Not started | ADR 0008 accepted; pending domain, data/sync, sales, and stock-integration foundations. |
| 14 — Reports | Not started | Pending completed sales. |
| 15 — Appointments demo | Not started | Local-only demo scope. |
| 16 — On-device voice assistant | Not started | MVP scope; stable iOS 26 Apple APIs, typed drafts, zero conversational retention, manual fallback, and an 8-hour field trial. |
| 17 — Hardening | Not started | Pending all MVP features, including the local assistant and its recovery/privacy evidence. |
| 18 — QA/release | Not started | Pending hardening; closes the MVP. |
| 19 — GPT-5.6 Luna | Post-MVP | Prepared behind the phase 16 contract; requires a fresh provider/privacy/cost ADR after the MVP and the Foundation Models field report. |

## Verified Bootstrap Baseline

- Host: macOS 26.6 (`25G5065a`). Xcode: 26.6 (`17F113`). Validated shared schemes/test plans: `FranAlonso-Develop` and `FranAlonso-Production`.
- The validated Debug builds use the iPhone Simulator 26.5 SDK. Project, app, and test `Debug-Develop`, `Release-Develop`, `Debug-Production` and `Release-Production` deployment targets are aligned to iOS 26.0 without raising the app minimum.
- App and test targets use Swift language mode 6.0, `SWIFT_STRICT_CONCURRENCY = complete`, and `SWIFT_DEFAULT_ACTOR_ISOLATION = nonisolated` in all four configurations. This keeps Domain/Data actor-neutral by default while UI-owned types use explicit `@MainActor` isolation or inherit it from a global-actor-isolated framework protocol such as SwiftUI `App`.
- The app supports iPhone and iPad, with Mac Catalyst, Designed for iPhone/iPad on Mac, and Designed for iPhone/iPad on visionOS disabled.
- Firebase 12.16.0 is resolved through the shared `Package.resolved` file.
- The exact approved Firebase products are linked: Core, Auth, Firestore, Storage, Analytics Core, and Crashlytics. `FirebaseAnalyticsIdentitySupport`, AdSupport, and IDFA capability are absent; `-ObjC` is present as required by the Firebase integration guide.
- The SwiftUI entry point registers an app-owned `UIApplicationDelegate` through `UIApplicationDelegateAdaptor`. Its launch callback calls the free `configureFirebase()` function directly and publishes only whether configuration succeeded; no injected bootstrap closure, intermediate bootstrap object or configuration protocol exists. `AppRuntime` uses that signal and the explicit build environment to compose the Firestore Clients, Products, Services and Sales adapters and their independent SyncEngines after bootstrap. All four engines remain inactive and perform no remote access until a later approved trigger calls them. Firebase SDK imports remain inside Data/bootstrap infrastructure boundaries; Auth and Storage stay unimported until their real adapters are introduced.
- `AppDelegate` lives in its own `App/AppDelegate.swift` file. `Resources` contains the String Catalog, `Assets.xcassets` and the `franalonso.icon` and `franalonso-develop.icon` Icon Composer documents. The local-only Firebase plists live outside the synchronized app group under `Configuration/Firebase/{Develop,Production}`; an app build phase validates the selected plist bundle ID and Firebase project ID, then installs exactly one canonical `GoogleService-Info.plist` before Crashlytics.
- Analytics is permanently deactivated in the versioned base Info.plist because Firebase's unavoidable automatic event stream cannot satisfy the accepted closed allowlist; automatic screen reporting and IDFV collection are also disabled. Its Firebase adapter fails closed if asked to enable or log. Crashlytics remains opt-in. The telemetry contracts expose only typed, versioned technical events/diagnostics, gate writes on explicit consent, and absorb adapter failures independently.
- Develop and Production Debug/Release configurations generate DWARF with dSYM files. The final Crashlytics build phase declares the app dSYM, executable, packaged Firebase plist, and Xcode debug-dylib sandbox inputs.
- Xcode recommended settings were accepted manually before 02.1; the resulting `LastUpgradeCheck` and String Catalog symbol-generation settings build without warnings.
- All four project configurations enable `SWIFT_TREAT_WARNINGS_AS_ERRORS` and `GCC_TREAT_WARNINGS_AS_ERRORS`; the app and test targets inherit both settings.
- The project contains one application target and one Swift Testing unit/integration target. `FranAlonsoTests` contains 509 enabled `@Test` declarations that currently expand to 598 passing results under `FranAlonso-Develop`; the last environment-specific baseline passed its then-current 163 declarations and 177 results under both schemes. There is no UI-test target, native UI-test source, or XCTest/XCUITest import.
- `ModelContainer.production(for:migrationPlan:)` and `ModelContainer.inMemory(for:migrationPlan:)` create containers from an explicit `Schema` and optional migration plan through an extension on the type they produce. Both paths use one private shared construction path and a writable configuration with App Group and managed CloudKit storage disabled; Firestore remains the accepted remote authority. The app does not invent a bootstrap persistence model or attach a schema-less container before real feature models exist.
- `AppDependencies` owns the shared `TelemetryReporter` plus the observe UseCases for Clients, Products, Services and Sales and the existing `SaveSaleUseCase`; it composes concrete dependencies only in `App` and requires all four Repositories explicitly at its initializer boundary. `live(modelContainer:)` constructs the four default Repositories from independent persistence actors and observation signals, while `preview(clients:products:services:sales:)` remains deterministically seeded in memory without live services. `AppEnvironment.swift` exposes the single typed container through SwiftUI's `@Entry`; telemetry previews remain private no-op data sources. `FranAlonsoApp` creates one production `ModelContainer` before composing and injecting its live dependencies. There is no singleton, service locator, extra environment wrapper, direct repository access from a View, or live Firebase dependency in tests or previews.
- `ContentView` is the current composition boundary for the Clients vertical: it reads the typed `AppDependencies` environment value and passes only `ObserveClientsUseCase` into `ClientListScreen`. The Screen creates and retains one `ClientListViewModel` with `@State`, owns its lifecycle-bound `.task`, and delegates rendering to a state-only `ClientListContent`; `ClientRow` renders immutable Domain data with stable `Client.id` identity. No Store is introduced because the screen still has one cohesive state responsibility.
- Spanish (`es`) is the project and String Catalog source language. `Localizable.xcstrings` uses stable semantic keys documented in `docs/LOCALIZATION_GUIDE.md`; the app consumes Xcode-generated `LocalizedStringResource` symbols with symbol generation enabled in all four configurations.
- The app bundle contains three stable, semantic, single-page A4 PDF resources: consent, ticket and invoice. Their source content and deterministic ReportLab generator are versioned, while PDFKit previews prove all three load through the same `DocumentTemplateResource` contract that phase 08/13 can reuse.

## Observed Bootstrap Gaps

- `Configuration/Firebase/Develop/GoogleService-Info.plist` and `Configuration/Firebase/Production/GoogleService-Info.plist` remain local, ignored, untracked, parseable and aligned with their respective app bundle identifiers and the approved Firebase project. A clean clone or CI environment must provision both securely; a missing file, bundle mismatch or project mismatch deliberately fails that environment's build instead of falling back to Production.
- The distinct bundle identifiers isolate each installed app's sandbox, SwiftData store and local Firebase session, but both Firebase apps still share the project's Authentication, Firestore and Storage backends. Firestore environment paths, future Storage prefixes and Security Rules remain mandatory logical boundaries; the configuration step itself deployed no Rules, and the later provisional Firestore Rules follow-up is recorded below.
- Firebase Analytics remains intentionally non-operational. Enabling its SDK-managed automatic events requires either a newly accepted explicit allowlist/ADR or a different adapter that can enforce the existing closed allowlist; ordinary consent alone must not activate Firebase Analytics.
- Crashlytics collection overrides persist across launches and `setCrashlyticsCollectionEnabled` does not apply its new value until the next run. The base plist keeps collection off when no persisted runtime override exists, and 02.8 creates no production grant or override call. Before opt-in is implemented, persisted owner consent, next-launch behavior, pending-report handling, and migration of any stale development override must be designed and tested explicitly; a SwiftUI task cannot act as a current-launch privacy gate.
- Firebase app-delegate swizzling is disabled. Firebase Messaging, push entitlements, notification permissions, background modes, APNs token mapping, visible-notification delegation, and silent-notification callbacks are intentionally not added in 02.6; if Messaging or Firebase Auth phone flows are introduced later, their required application-delegate callbacks must be implemented and tested explicitly.
- No live controlled Crashlytics event was sent: automatic collection is intentionally disabled and the official Xcode MCP surface cannot perform the required debugger-detached launch. The local dSYM bundle, UUID match, packaged plist, and synchronous build-phase validation are complete; dashboard receipt remains an explicit opt-in operational smoke check and Crashlytics must not be described as live until it succeeds.
- The production `Schema.franAlonso` is built from the versioned `PhaseFiveBaselineSchema` `1.0.0` and contains twenty-eight models: seven explicit entity/pending-operation/remote-state/conflict/cursor/retry tables for each of Clients, Products, Services and Sales. `FranAlonsoApp` composes the single-schema, zero-stage migration plan explicitly. Their local DataSources, persistence actors, local-first Repositories, contextual main-context adapters, neutral remote contracts, conflict policies and independent SyncEngines support tombstone convergence, atomic cursor advancement, bounded durable backoff and cooperative cancellation. Sales permits only draft discard and retains progressed remote state as conflict. The Firestore adapters are composed only after successful bootstrap and remain inactive; an automatic trigger remains intentionally absent until a later approved scope.
- The consent PDF is intentionally marked as a draft pending legal review. Its current text follows the RGPD/LOPDGDD information-by-layers structure, but production use remains blocked until Fran Alonso and the responsible lawyer approve the actual purposes, processors, transfers and retention wording.
- The document wordmark is a provisional vector approximation pending an original brand asset. Replacing it does not change resource names or consuming code.
- The supplied signature-and-stamp photograph remains outside Git and the application bundle. Phase 13.6 must provision it only after authentication, validate it and cache it with file protection; documents remain renderable without it.

## Latest Evidence

- Phase 07 readiness preflight on 2026-08-10 confirms that local `main` and `origin/main` are identical at `3f58314`, the worktree was clean before this documentation update, only the local and remote `main` references remain, and no `AppSection`, `AppShellViewModel`, `AppShellScreen`, `.sidebarAdaptable` or production palette implementation exists outside the approved specifications and design documents. Obsidian CLI resolves the open `FranAlonso` vault to this repository root and reads `docs/Progress.md` directly, so no separate project-status note requires synchronization. Fresh operational verification remains incomplete: the official Xcode MCP connection returns `Transport closed`, while the Linear connector still requires OAuth reauthentication. These connector failures do not invalidate the last audited 780/780 and warning-free build baseline, but executable phase 07 work remains gated on restoring both live checks and completing the source-backed proposal, its independent read-only review and explicit owner approval.
- PLU-24 is complete and its dedicated `codex/plu-24-swift-source-formatting` branch has been integrated and removed, remaining intentionally separate from phase 07. The approved horizontal-first policy is now explicit in `AGENTS.md`, the development guide and the pull-request checklist: declarations stay horizontal through three simple parameters when the complete signature fits within 120 columns; complex or four-or-more-parameter signatures stay vertical with compact inner types; typed bindings stay horizontal when they fit; compact guards are limited to one simple immediate exit; and complete one-line functions are limited to manually reviewed pure predicates, accessors, adapters and test doubles. The historical pass adjusted 366 declarations, 20 typed bindings, 196 guards and 47 trivial helpers across 128 Swift files, plus six long test expectations. A SwiftSyntax lexical comparison proves every source and comment token identical to its pre-formatting baseline; the ordinary diff was reviewed and `git diff --check` is clean. Nineteen lines remain above the preferred width exclusively because they are atomic raw JSON fixture literals whose content must not be changed by a formatting task. On the definitive corrected tree, Xcode MCP built `FranAlonso-Develop` in 8.647 seconds with zero warnings, and its complete active plan passed 780/780 tests with no failures, skips, expected failures or unexecuted results. The final general and SwiftUI/accessibility audits report no open P0–P3 findings; preview rendering is proportionally reused from the token-identical baseline and its documented Large, XXX Large and AX 5 evidence. PR [#3](https://github.com/JFrancoG/FranAlonso/pull/3) integrated the delivery through `3f58314`; local `main` and `origin/main` are synchronized at that commit and no source branch remains.
- Phase 06 closed through [PR #2](https://github.com/JFrancoG/FranAlonso/pull/2), which rebased all sixteen phase commits onto `main` and produced the definitive integrated tip `c368829`. GitHub reported the PR mergeable and clean, with no configured status checks, submitted reviews or unresolved threads. Before branch cleanup, the rebased `origin/main` tree was byte-equivalent to the published phase branch and `git cherry` matched all sixteen patches upstream. Local `main` was fast-forwarded, then both branch references were deleted; validation is reused from the identical audited tree described below.
- Subphase 06.7 composes the owner-approved authentication root only after Firebase bootstrap succeeds. `AuthenticationRootViewModel` owns the login and authoritative-session ViewModels and presents exactly one checking, signed-out, locked, authorizing, authenticated, denied, signing-out or observation-failed root. A recent email/password result is only ephemeral evidence: the stream must publish the same UID before `AuthorizeLocalPrincipalUseCase` can grant access. Restored sessions require biometrics, while a person whose biometrics is unavailable can sign out and authenticate again with email/password. `SessionViewModel` records a monotonic local-access revision inside the ordered stream consumer; every `nil`, different UID, observation replacement or retry makes earlier credential proof, in-flight authorization and granted access stale even if SwiftUI coalesces presentation updates. Cancellation also clears current recent-credential proof, while the authorization revision prevents an obsolete task from clearing a newer replacement. Accepted ADR 0021 owns this policy. Data binds the opaque UID in a non-synchronizable `kSecClassGenericPassword` item accessible when unlocked. A missing item can be claimed only after `SwiftDataStorePristineDataSource` proves all 28 published model tables empty with fetch limit one; rows or errors fail closed, `SecItemAdd` races reread the exact bytes, and logout never changes the binding. `AppRuntime` composes Firebase Auth, LocalAuthentication, Keychain and the SwiftData actor exactly once after bootstrap; all four SyncEngines remain inactive. TDD initially failed on the absent root, authorization, adapter, bootstrap and composition symbols before the focused GREEN sets passed. The audit correction reproduced a security race at 0/2: consecutive `nil → same UID` and `other UID → original UID` events reopened an earlier shell; the stream-owned revision now passes both at 2/2. A cancellation regression then failed 0/1 because credential proof survived the cancelled authorization and now passes 1/1; the root suite passes 13/13. The complete `FranAlonso-Develop` plan passes 780/780 from 609 declarations; the app builds in 6.870 seconds with no structured errors/warnings or Navigator issues, and catalog, layer, forbidden-API/concurrency, width and whitespace checks are clean. `AuthenticationRootScreen`, `LoginScreen`, `SessionScreen` and `ContentView` render and were inspected in Large, XXX Large and AX 5. Inert previews additionally prove local denial, observation failure and the authenticated shell in all three variants. The first architecture audit found the coalesced-event P1, stale Progress P2 and redundant explicit `Sendable` P3; the first SwiftUI audit found the missing root-state-preview P3. All corrections are implemented and both focused re-audits report no open P0–P3 findings. Live Firebase accounts, Keychain, the production SwiftData store, real biometrics, VoiceOver, AutoFill and hardware keyboard remain explicit operational/manual residuals. No schema migration, Rules/App Check change or SyncEngine activation is included. The complete audited functional scope is published in `44abac1`.
- Subphase 06.6 implements the exact owner-approved login and authoritative-session presentation gate after its source-backed proposal passed independent review. `LoginScreen` owns `LoginViewModel`, binds email and password, and converts explicit user intent into one cancellable `.task(id:)` request whose identity is cleared after completion so view reappearance cannot repeat it. `SessionScreen` renders the same externally owned `SessionViewModel`, localizes the biometric reason from the current locale and applies the same one-shot identity rule to unlock and sign-out; root composition and the production authority that calls `load()` remain deferred to 06.7. `LoginContent` and `SessionContent` expose native persistent labels, AutoFill hints, keyboard/submit focus, explicit progress, neutral failures, biometric availability, locked/unlocked/signed-out/observation states and the always-visible email fallback without validation or provider policy. Every View has deterministic same-file previews through `AppPreviewModifier`; the Screen preview remains deliberately in the stable initial checking state, while `SessionContent` owns deterministic locked, unlocked, signed-out and error coverage through immutable non-live fixtures. The catalog adds 36 manually translated Spanish authentication keys with comments and generated symbols. RED failed for the expected absent symbols and localized failure mappings; GREEN's four declarations expand to 19 results. The first architecture audit found only stale progress evidence. The first SwiftUI/accessibility audit found three P3 issues: non-trailing closure calls, a Touch ID-specific symbol and duplicated accessible field labels. The corrections use native trailing closures, a modality-neutral unlock symbol and visually persistent labels hidden only from the accessibility tree; one-shot action identity was also hardened against reappearance. The architecture re-audit is clean. The first focused SwiftUI re-audit found one additional P3 because the Screen preview raced its asynchronous observation; the preview no longer calls `load()`, therefore has one stable state, and the final focused re-audit confirms the correction with no remaining finding. Validation passes the focused 9/9 Xcode selection and the complete 740/740 plan. After the preview-only correction, the app rebuilds in 17.488 seconds with 0 structured warnings or Navigator issues; `SessionScreen` reports 0 diagnostics, the five production files had already reported 0 diagnostics, the test compiles and passes despite Source Editor diagnostic error 5, and all static checks remain clean. `LoginScreen` and `SessionScreen` render Large, XXX Large and AX 5 without errors; the stable `SessionScreen` preview is repeated in XXX Large, and focused AX 5 locked and error states remain legible. Both final specialist re-audits report no open P0–P3 findings. VoiceOver, AutoFill, a physical keyboard, a device biometric prompt, application-root composition and all live Firebase behavior remain explicit residual work; Xcode reports no supported localization preview override, so RTL was reviewed statically. The complete audited functional scope is published in `0c98ab4`.
- Subphase 06.5 implements the exact owner-approved `@Observable @MainActor` authentication presentation models after its corrected source-backed proposal passed the independent pre-implementation review with no findings. `LoginViewModel` owns only ephemeral email/password input and explicit idle, loading, successful and neutral failed intent states; it rejects duplicate submissions, clears the password after provider success, preserves cancellation as idle and never treats intent success as root session authority. `SessionViewModel` consumes the authoritative observation stream, locks every new principal behind freshly queried biometrics, preserves same-principal lock/action state, waits for observed `nil` after successful sign-out and uses observation revisions plus action identity to reject replacement, cancellation, duplicate/crossing and stale completions. RED first failed because `LoginViewModel` was absent, then because `SessionViewModel` was absent. The audit correction added another RED in which stale biometric availability incorrectly unlocked the session; GREEN now refreshes availability for every admitted attempt and does not evaluate unavailable biometrics. The test stream double confirms relevant same-principal elements were actually consumed instead of relying on counted scheduler yields. The twenty-eight new declarations expand to 45/45 results in the complete plan, which passes 721/721. The final Xcode MCP build succeeds in 6.804 seconds with 0 structured warning or Navigator issues; all four files report 0 diagnostics, and static layer-import, forbidden-concurrency/API, line-width and whitespace checks are clean. The first post-implementation architecture/concurrency audit reported the two P2 findings just described; both corrections pass 3/3 focused tests, and the required focused re-audit closes them with no findings. SwiftUI/accessibility is `N/A` because no View, preview, localization, visual resource or accessibility surface changed. App composition, screens, root gating, live Firebase, account provisioning, Rules/App Check, session freshness/revocation, local-data ownership and SyncEngine activation remain deferred. The complete audited functional scope is published in `e0c1cbf`.
- Subphase 06.4 implements the exact owner-approved local biometric session-unlock capability under accepted ADR 0020 after the source-backed proposal passed its independent pre-implementation review with no findings. The final owner-approved organization groups the Firebase and LocalAuthentication implementations directly under `Authentication/Data/Adapters`; it changes no executable declaration or behavior, so an additional RED/GREEN cycle is `N/A`. Domain owns the concrete `BiometricAuthenticator` value and stable denied, cancelled, unavailable, configuration and unexpected errors; it rejects an empty localized reason and preserves Swift task cancellation before and after provider work. The live Data factory creates a fresh actor-confined `LAContext` for every attempt, checks and evaluates only `.deviceOwnerAuthenticationWithBiometrics`, suppresses the passcode fallback action and invalidates the context when its parent task is cancelled. It stores no password and accepts neither device passcode nor a companion device. The versioned Info.plist and Spanish InfoPlist String Catalog provide the Face ID purpose text for unlocking an already established session. RED: the focused Xcode MCP test build failed on the deliberately absent `BiometricAuthenticator`, `BiometricAuthenticationError` and policy-evaluation symbols. GREEN: ten declarations expand to 23/23 focused results. After the structural move, the Firebase Auth plus biometric selection passes 38/38 and the complete `FranAlonso-Develop` plan passes 676/676 from 547 declarations. The final Xcode MCP build succeeds in 7.507 seconds with 0 structured warnings or Navigator issues, and both moved files report 0 diagnostics. The built app contains the exact `NSFaceIDUsageDescription` value and matching `es.lproj/InfoPlist.strings`; plist, catalog, forbidden-concurrency/API, adapter-import, path and whitespace checks are clean. The first localization audit reported one P3 because the catalog comment did not identify the system prompt; the comment now names that UI location, and its focused re-audit closes the finding. The functional architecture/data/concurrency audit, localization re-audit, structural proposal review and final structural implementation audit all report no findings. SwiftUI previews are `N/A` because no View changed; a physical Face ID prompt remains a later manual validation. App composition, ViewModels, UI/root gating, live Firebase, Rules/App Check, session freshness/revocation, local-data ownership and SyncEngine activation remain deferred. The complete functional scope is published in `b8efeaf`, with checkpoint documentation in `d617576`.
- Subphase 06.3 implements the exact owner-approved Firebase Auth adapter after the corrected source-backed proposal passed its independent pre-implementation review with no findings. The immutable `FirebaseAuthenticationDataSource` stores three explicit `@Sendable` operation closures, forwards credentials without retaining or logging them, reduces `AuthDataResult` to the opaque UID, preserves injected cancellation and treats an already delegated Firebase result as authoritative. Its stable error map first guards `AuthErrors.domain`, covers all sixteen approved Firebase 12.16.0 codes and collapses unknown or unrelated failures to `.unexpected`. The live initializer resolves `Auth.auth()` only when explicitly constructed after Firebase configuration and remains uncomposed until 06.7. `authStateChanges` is relayed losslessly through the provider-neutral `AsyncStream`; downstream finish, cancellation, abandonment before iteration and early `break` all release the lifecycle-bound relay without a callback bridge or unsafe concurrency escape. RED: the focused Xcode MCP test build failed only because `FirebaseAuthenticationDataSource` did not exist. GREEN: fifteen declarations expand to 30/30 focused results, including all sixteen parameterized error cases, and the complete `FranAlonso-Develop` plan passes 653/653 from 537 declarations. The first post-implementation audit found one P2: under `NonisolatedNonsendingByDefault`, the synchronous Firebase sign-out/Keychain path could block a `MainActor` caller. The concrete witness is now `@concurrent`, and an Xcode probe confirmed that it remains a valid `AuthenticationDataSource` witness while explicitly leaving the caller actor. The final focused re-audit reports no findings. The final Xcode MCP build succeeds in 7.711 seconds with 0 structured warnings or Navigator issues, and both new Swift files finish with 0 diagnostics after one transient SourceEditor error 5. `git diff --check` plus Firebase-import, credential-log and unsafe-concurrency/API scans are clean. The raw log retains the pre-existing AppIntents metadata-skipped warning that Xcode does not classify as a structured issue and that does not originate in this diff. SwiftUI/accessibility review is `N/A` because no View, preview, localization, visual resource or accessibility surface changed. Live Firebase, Console configuration, App composition, UI, biometrics, Rules/App Check, token freshness/revocation, local-data ownership and SyncEngine activation remain deferred. The complete functional scope is published in `eb26bce`.
- Subphase 06.2 implements the exact owner-approved provider-neutral Data boundary after a final fresh pre-implementation review returned no P0–P3 findings. `AuthenticationDataSource` returns Domain `AuthenticationSession` values directly and exposes stable credential, account, network, throttling, configuration, secure-storage and unexpected failures. `DefaultAuthenticationRepository` translates those failures into `AuthenticationError`, preserves `CancellationError` before fallback mapping and returns the upstream session stream directly. There is no identity-only DTO, Mapper, buffering policy, mapping task, Firebase import or production in-memory Repository. RED failed because the new contract tests referenced the absent DataSource, error and default Repository symbols. GREEN initially passed 23/23 focused results from eleven declarations. The first post-implementation audit then found one P2 because the undeclared-error fallback and cancellation-preservation branches duplicated by `signOut` lacked direct coverage. Two deterministic tests close that gap without changing production code: the thirteen declarations now expand to 25/25 results and the full `FranAlonso-Develop` plan passes 623/623 from 522 declarations. Xcode MCP builds in 8.256 seconds with no structured warning-level build-log or Navigator issues, and all three new Swift files report 0 diagnostics. Static forbidden-import, DTO/Mapper, buffering, unsafe-concurrency/API and whitespace checks are clean. The raw log retains the pre-existing AppIntents metadata-skipped warning that Xcode does not surface as a structured warning and that does not originate in this diff. Concrete Firebase error mapping, real listener cleanup and early-abandonment behavior remain explicitly deferred to 06.3. SwiftUI/accessibility review is `N/A`; the final focused architecture re-audit confirms the P2 closed with no P0–P3 findings or regressions. The complete 06.2 scope is published in `78d5cca`.
- Subphase 06.1 is an owner-approved Domain-only authentication slice under accepted ADR 0019, published on the phase branch in `f6c0db7`. The fresh pre-implementation review required the provider decision, post-delegation cancellation guarantee and credential/privacy behavior to be explicit; the corrected source-backed proposal then received owner approval. `AuthenticationSession` exposes only a stable opaque identifier; `AuthenticationRepository` defines email/password sign-in, sign-out and an authoritative session `AsyncStream`; `SignInUseCase`, `SignOutUseCase` and `ObserveSessionUseCase` preserve the approved validation, delegation and cooperative-cancellation semantics. `AuthenticationError` keeps invalid credentials and a disabled account distinct for policy while requiring the future presentation layer to disclose neither. Accounts remain externally provisioned, and signup, reset, federated/phone providers, UI/localization, Firebase adapters/configuration, live users, Rules/App Check, SyncEngine activation and local-data migration are excluded. RED failed because the new tests referenced the absent authentication symbols. GREEN passes the twelve focused tests 12/12 and the full `FranAlonso-Develop` plan 598/598 from 509 declarations. The Xcode MCP app build succeeds in 8.903 seconds with no warning-level build-log or Navigator issues; all six affected Swift files report 0 diagnostics, and forbidden-import, unsafe-concurrency/API and whitespace checks are clean. The final independent architecture review verified the full diff and primary-source applicability with no P0–P3 findings. Its residual gates are the deliberately deferred Data/Firebase adapter, listener cleanup, exhaustive SDK error mapping, live configuration/provisioning and enumeration protection, revocation, local-data isolation, biometrics, UI and root gating. The raw build log contains the pre-existing `warning: Metadata extraction skipped. No AppIntents.framework dependency found` message; Xcode MCP does not surface it as a warning-level build-log entry or Navigator issue, and it does not originate in this diff. SwiftUI/accessibility review is `N/A` because no View, preview, localization, visual resource or accessibility surface changed. No PR or merge was created.
- PLU-14 semantic DocC backfill documents the existing telemetry privacy boundary, the client-list state transition, and the deterministic snapshot/identity semantics of the four in-memory repositories. The Swift scope is ten production files with 89 documentation-only insertions and no executable declaration change; obvious stored properties, enum cases, view boilerplate, mechanical serialization, trivial helpers and tests remain intentionally undocumented. RED/GREEN, Xcode MCP build and Swift Testing are `N/A` because executable behavior did not change. A synthetic Swift 6 module compiled 38 Domain/support files with warnings as errors, emitted an internal symbol graph and converted through DocC with warnings as errors. `git diff --check`, placeholder, long-comment and non-DocC Swift-diff scans are clean. The independent full-diff architecture review returned no P0–P3 findings; its residual limits are that Firebase adapters were source- and SDK-reviewed outside the pure module and identity replacement has direct representative test coverage in Clients. SwiftUI/accessibility review is `N/A` because no View, preview, localization, visual resource or accessibility surface changed. This maintenance does not advance the phase 06.1 gate or activate any SyncEngine.
- Phase 05 delivery: the complete audited history was fast-forwarded into `main` through `20a4dde`, preserving every subphase commit without a merge rewrite. Final executable evidence remains the 586/586 Swift Testing result from 497 declarations, the 10.582-second warning-free Xcode MCP build, zero production-file diagnostics and the independent audit with no P0–P3 findings. The subsequent closure changes affect only this progress record and the changelog; phase 06.1 is the next gate.
- Checkpoint 05.11 adopts the exact 05.10c shape as `PhaseFiveBaselineSchema` version `1.0.0` and composes `PhaseFiveSchemaMigrationPlan` with that sole schema and no stages. ADR 0018 supersedes ADR 0017 after its first raw gate proved that all internal checkpoint shapes shared the persisted `1.0.0` identifier; the owner confirmed that no earlier build carried local data requiring preservation. There is no generic opening bridge, hash heuristic, custom stage, repair, reset or reseed.
- Revised RED failed only because `PhaseFiveBaselineSchema` was absent. GREEN writes a raw store with one representative row in each of the 28 current tables, then opens and reopens it through the product schema and plan while comparing business snapshots, pending causal metadata, remote records, conflicts, cursors and retry state for all four verticals. The two new tests pass 2/2, all seven selected migration regressions pass 7/7 and the full plan passes 586/586 from 497 declarations. Xcode MCP builds in 10.582 seconds with zero warning-level log or Navigator issues and zero diagnostics in the three production files; the isolated test-file refresh reports Source Editor error 5 despite successful compilation. Static prohibited-API and whitespace checks are clean. The final independent iOS audit reports no P0–P3 findings; SwiftUI/accessibility is `N/A` because no View, preview, localization, visual resource or accessibility surface changed.
- Checkpoint 05.10c implements the exactly approved Sale Data vertical under accepted ADR 0016. `SaleDTO` preserves the complete ordered aggregate and uses strict tagged lifecycle payloads; `SaleTimestampDTO` transports every finite date as a canonical sixteen-character IEEE-754 bit pattern, and Domain rejects nonfinite creation/payment/closure/reversal dates before mutation or decoded reconstruction. `CanonicalDecimalDTO` replaces the Service-owned name without an alias or wire-format change, and both Sale and Service rerun Domain validation at their mapping boundaries.
- Sales owns seven explicit SwiftData models, local and Firestore DataSources, `SalePersistenceActor`, contextual main-context adapter, local-first Repository, observation signal, full-snapshot policy, cursor, durable retry and `SaleSyncEngine`. `SaleModel` flattens queryable root metadata while keeping ordered lines in a versioned Codable payload and verifies that the queryable `Date` agrees bit-for-bit with its canonical value. A tombstone means only draft discard: there is no public delete UseCase, local and remote paths both reject progressed Sales, and conflict persistence retains the discard operation plus the progressed remote record.
- `FirestoreCollection.sales` resolves each environment's Sales collection and independent sync counter. Every write plan pairs one complete Sale document or business-field-free tombstone with the next counter value; the transaction reads the authoritative Sale before accepting discard. App requires the fourth Repository explicitly, exposes Sales observe/save UseCases and composes the independent engine only after Firebase bootstrap. No caller invokes `synchronize()`, and no Rules, index, live document or traffic changed.
- The first architecture/data/concurrency audit found one P1, four P2 and three P3. Corrective RED proves a discard cannot skip its captured base or predecessor; `-0.0` persists as canonical `+0.0`; every pending base requires its explicit v1 envelope; unknown Firestore root keys fail closed while modern and legacy shapes remain valid; invalid identifiers retain the exact aggregate and indexed Firestore path; equal creation dates use stable Sale identity; the modified lifecycle DocC and test fixture structure now match repository rules.
- RED first failed on absent finite-date validation, canonical decimal/Sale transport and vertical symbols. Further REDs proved that nested status/line keys were too permissive, six audited behaviors failed in a 7-test selection and the location-aware UUID contract was absent at compile time. GREEN passes the original 140/140 non-overlapping focal results, corrective 7/7 and 2/2 selections, and the final 584/584 full plan from 495 declarations. Xcode MCP builds `FranAlonso-Develop` in 5.434 seconds with 0 warning-level build-log or Navigator issues; the seven corrected production files report 0 diagnostics. `git diff --check` and static layer, API, serialization and concurrency scans are clean.
- The real file-backed migration creates the exact twenty-one-model Clients+Products+Services published store with representative business rows plus pending, remote, conflict, cursor and retry metadata, reopens it under all twenty-eight models, preserves every prior value and starts all seven Sale tables empty. Security Rules retain their previous 23/23 evidence and the new namespace remains closed. The final architecture/data/concurrency re-audit confirms all eight findings closed with no regressions; the independent SwiftUI/accessibility audit reports no findings and marks previews, RenderPreview and VoiceOver `N/A` because no visual or semantic surface changed. 05.10c is published through `379bf76`; its then-next 05.11 gate is now governed by ADR 0018 and the newer evidence above.
- Checkpoint 05.10b implements the exactly approved Service Data vertical under accepted ADR 0015. Service owns explicit DTO/mapping, seven SwiftData models, local and Firestore DataSources, persistence actor, local-first Repository, observation signal, complete-snapshot conflict policy, tombstones, cursor, durable retry and `ServiceSyncEngine`. Price, tax and optional discount cross the DTO and persistence boundaries as validated canonical base-ten strings; mapping re-runs every Domain invariant and rejects any monetary normalization that would change the remote amount.
- `FirestoreCollection.services` resolves `develop/collections/services`, `production/collections/services` and each environment's `collections/syncMetadata/services` counter. Every planned mutation keeps the Service document and counter in one transaction. App composes the independent Service Repository and creates its engine only after Firebase bootstrap, but no caller invokes `synchronize()`: no trigger, background task, Rules change, document read/write or access to manually created data occurred.
- Only the already source-identical retry values and pure scheduling calculation moved to `Shared/Data/Sync`; Client, Product and Service retain feature-specific remote errors, durable retry rows, persistence callbacks, three-attempt orchestration, cancellation checks and explicit engines. The focused 05.10b selections pass 139/139, and dedicated extraction regressions pass 19/19 for Client plus 21/21 for Product.
- RED first failed on the absent canonical decimal contract and then on the absent Service Data/sync/App symbols. GREEN finishes with 438 enabled declarations and 505/505 full-plan results. Xcode MCP builds `FranAlonso-Develop` in 6.061 seconds with 0 warning-level log or Navigator issues; 17 critical files report 0 diagnostics. The file-backed test reopens the exact fourteen-model published schema under twenty-one models, preserves every prior Client/Product table and begins with all seven Service tables empty. Static API/import/concurrency/serialization/whitespace checks and `git diff --check` are clean.
- The independent SwiftUI/accessibility audit found no findings and marked previews, RenderPreview, VoiceOver and semantic accessibility as `N/A`: no View or visual surface changed, and the only lifecycle addition composes an inactive, idempotent engine with zero remote calls. The architecture/data/concurrency audit found no P0–P2; its two P3 documentation findings —stale live evidence and a historical 05.10a statement written in the present tense— were corrected, and the final focal check closed without findings. The checkpoint is published through `0cc915d`; 05.10, PLU-12 and phase 05 remain open for 05.10c Sales and 05.11.
- Checkpoint 05.10a implements the owner-approved Product Data vertical under accepted ADR 0014. Product owns explicit DTO/mapping, seven SwiftData models, local and Firestore DataSources, persistence actor, local-first Repository, observation signal, causal policy, tombstones, cursor, durable retry and `ProductSyncEngine`; no generic cross-collection persistence or sync abstraction was introduced. Its only business fields are `id`, `name` and `status`; StockMovement, Services and Sales remain outside this checkpoint.
- At the 05.10a checkpoint, `FirestoreCollection` contained only the two concrete adapters `.clients` and `.products`, while `FirestoreEnvironment` owned their business and sync-metadata paths. App composed independent Client and Product Repositories and created each inactive SyncEngine only after Firebase bootstrap. Engine construction performed no network operation, `synchronize()` had no caller, Rules remained unchanged and the manually created `develop` client plus all Product documents remained untouched. ADR 0014 requires a separate approved live gate with complete-writer inventory, Rules/emulator contracts, contention evidence and a reversible smoke test.
- RED first failed on absent Product DTO/local/persistence/sync symbols and then on absent `AppDependencies.observeProducts`; GREEN passes 104/104 Product results and 366/366 overall from 335 declarations. After the final P3 correction, Product passes again 104/104 and Xcode MCP builds `FranAlonso-Develop` in 5.412 seconds with 0 build-log warnings or Navigator issues; the prior five-file diagnostic pass found 0 issues. A file-backed store containing all seven exact Clients 05.9 models reopens under the fourteen-model schema with every Client business/metadata snapshot preserved and all seven Product tables empty. Static API/import/concurrency/serialization/whitespace checks are clean. The final read-only audit found no P0–P2 and two P3: unused compatibility aliases and the stale update date; both were corrected and the focused re-audit returned no findings. SwiftUI/accessibility is `N/A`. The checkpoint implementation is published in `6c89e0e`.
- Subphase 05.9 implements accepted ADR 0013 with one durable retry row per pull or immutable pending operation. `UNAVAILABLE`, `DEADLINE_EXCEEDED` and `ABORTED` use exponential backoff with deterministic injected jitter, steps `1...6`, a 60-second ceiling and three external attempts per unit. Definitive failures clear stale backoff without discarding pending work. A new pass honors `notBefore` after restart; successful pull, direct acknowledgement or conflict clears its retry in the same local commit, including operation acknowledgement or conflict discovered by a later pull.
- The engine is single-flight through actor-isolated state and owns no `Task`. Cancellation wins before reconciliation and before failure classification; tests cover cancellation during the engine's nonblocking sleep, a provider callback that returns an error after cancellation and a remote commit that arrives after cancellation then converges idempotently on the next pass with the same `operationID`.
- Initial RED failed at compile time for the absent 05.9 symbols and initial GREEN passed 32/32 focused results. The final audit then exposed orphaned operation retries after pull reconciliation and a cancellation gap during injected timing; corrective RED failed 0/3 on those exact expectations and corrective GREEN passed 3/3. The complete Xcode MCP plan now passes 256/256 from 235 declarations, and `FranAlonso-Develop` builds in 4.725 seconds with no build-log or Navigator warnings. The exact file-backed 05.8 schema reopens with Client/cursor preserved and no retry rows; static and whitespace checks are clean. The repeat independent architecture/data/concurrency audit closed both P2 findings without new findings; SwiftUI/accessibility is `N/A`. No live Firestore access, Rules change, trigger or background task occurred.

- Subphase 05.8 completes the durable Clients queue with immutable pending deletes, PII-free remote tombstones and a persisted incremental cursor. Local delete hides the Client and operation in one commit; remote delete-wins prevents resurrection, ordinary upsert cannot restore a tombstone and a conflict keeps its full snapshots durable for explicit future recovery. Batch materialization and cursor advancement share one save/rollback boundary, negative or jumping cursors fail closed and a sequenced tombstone cannot be replaced by a stale live document.
- Accepted ADR 0012 keeps per-document revision as conflict authority and assigns `changeSequence` only to a per-collection change feed. A bootstrap with no cursor includes legacy documents; subsequent queries order and filter by sequence. Each remote live write or tombstone advances `<environment>/collections/syncMetadata/clients` and writes the Client document inside the same Firestore transaction. Counter absent starts at one; negative, malformed or maximal values yield no write plan. The shared counter's bounded MVP contention and rejected timestamp/change-listener alternatives are documented.
- Compatibility evidence reopens a file-backed store created with the exact 05.7 schema, preserving Client, pending upsert, remote state and conflict while the new delete/cursor tables begin empty. Exact 05.7 JSON bytes decode as `.live`. Initial and corrective RED/GREEN cover intermediate batch failure, actor restart, concurrent delete/edit, no resurrection, operation identity, publication before later failure, conflict descendants, invalid cursor and the atomic document+counter plan.
- Final validation: Xcode MCP lists 210 declarations and passes 224/224 results; `FranAlonso-Develop` builds in 6.426 seconds with 0 warnings or Navigator issues. Rules passes 23/23 including denial of the new sync-metadata counter paths, `npm audit` reports 0 vulnerabilities, and static/whitespace checks are clean. The final read-only architecture/data/concurrency re-audit closed without findings after promoting all four technical observations to regressions. SwiftUI/accessibility is `N/A`. No live Firestore traffic occurred, the manual `develop` client remains untouched, Rules remain closed and the engine remains inactive. 05.8 is published in `ae58f28`; 05.9 is the next proposal gate.

- Subphase 05.7 implements `ClientSyncPolicy` and the first `ClientSyncEngine` around immutable per-client causal chains. A changed local snapshot appends a successor instead of replacing an in-flight operation; acknowledging A deletes only A, preserves visible/pending B and lets B advance only after Firestore records A as its direct predecessor. Roots compare an exact absent, legacy-snapshot or positive-revision base; replay of the same operation and payload is idempotent, an operation-ID/payload mismatch is a conflict, and revision overflow is rejected. Full local/remote conflict snapshots are durable, block only their client and remove that chain from delivery while unrelated clients remain eligible.
- The Firestore adapter now decodes optional `_sync` metadata, treats a document with no metadata as legacy and rejects partial or invalid metadata. Each upsert runs inside the provider transaction, uses the stable client ID, `merge: true`, explicit nulls for known optional fields and a server-side revision increment, then reports success only through the transaction completion. This follows Firebase's documented [transaction retry/atomicity](https://firebase.google.com/docs/firestore/manage-data/transactions), [Swift Codable and `ExplicitNull`](https://firebase.google.com/docs/firestore/solutions/swift-codable-data-mapping) and [iOS bootstrap](https://firebase.google.com/docs/ios/setup) behavior. Firebase types remain inside Data.
- `AppRuntime` creates one persistence actor and observation signal shared by the local-first Repository and SyncEngine. `AppDelegate` publishes whether Firebase bootstrap succeeded; only then does the explicit `AppEnvironment` build value select `develop` or `production` and compose the remote adapter. Engine construction performs no fetch or upsert, and 05.7 deliberately adds no automatic synchronization trigger. Previews and the composing initializer retain inactive injected dependencies.
- RED evidence covered absent policy, causal persistence, engine and runtime symbols plus backward compatibility for 05.6 pending rows, conflict-delivery filtering and route validation before constructing a Firestore reference. GREEN passes 14/14 policy/persistence/engine/runtime results and 11/11 Firestore adapter results; the complete plan passes 202/202 from 188 declarations. The Xcode MCP build completes in 5.127 seconds with no warning-level build-log or Navigator issues. The expanded production container opens successfully without a business write; isolated file diagnostics return the known Source Editor error 2 while compilation and execution remain clean.
- Static checks find no `JSONSerialization`, `Any` dictionary, unsafe concurrency escape, legacy observation/GCD API, Domain infrastructure import or whitespace issue. The manually created `develop` client and all Clients data remain untouched. The final architecture/data/concurrency audit and the independent SwiftUI/accessibility audit both closed without findings. The latter confirmed that the lifecycle-only composition has no visual or semantic accessibility impact, so RenderPreview and VoiceOver evidence are `N/A`. Subphase 05.7 is complete locally.
- The owner-approved Firestore Rules follow-up replaces the former global `allow read, write` rule in `agendapeluqueria-3155f`. Because the legacy application shares this database and still lacks Authentication/App Check, the provisional rules preserve anonymous read/write only under `prod/prod`, `dev/dev` and `backup/backup`; a recursive catch-all denies the new `develop/collections/**`, `production/collections/**` namespaces and every unrelated path. RED ran the 23 cases against a local copy of the former global-open rule: all 13 legacy flows passed and exactly the 10 denial checks failed because every path was still allowed. GREEN passes official syntax validation and 23/23 emulator tests covering legacy CRUD/list, backup-to-production access, new and lookalike path denial and mixed-batch atomicity. The suite pins its external Firebase CLI prerequisite to 15.13.0 before starting the emulator. Firebase readback matches the versioned source. Storage, Auth, indexes and data were untouched; the disabled Service Usage API was not enabled when the CLI dry-run encountered it, and publication used the official Firebase console. The final read-only audit found the unversioned CLI prerequisite and missing RED evidence; after adding the exact runner guard and documentation, the focused re-audit closed without findings and `npm audit` remained at 0 vulnerabilities. The retained anonymous legacy roots are an explicit temporary risk until the incumbent app can migrate to authenticated least privilege in phases 06/17.

- Subphase 05.6 introduces a durable pending Clients upsert and composes `DefaultClientRepository` over SwiftData without a live remote dependency. `ClientLocalDataSource.persistPendingUpsert` owns the single save boundary for client materialization and pending-operation identity/payload; both the persistence actor and the `@MainActor` contextual adapter reuse it. `ClientObservationSignal` emits newest-only invalidations after a commit and every observer reloads SwiftData, so reordered actor messages cannot publish an obsolete snapshot. Identical payloads retain their retry identity and changed payloads replace the pair without adding another row. The source-backed proposal passed its mandatory read-only review and received exact owner approval before executable work. Initial RED failed on the expected absent symbols and GREEN reached 184/184. The final audit then reproduced two P1 defects: cross-actor snapshot publication could regress behind the store, and a mapping error after `save()` could report failure after accepting the write; it also found missing DocC on the live factory. The first corrective GREEN replaced snapshots with invalidations, moved full mapping before save and reached 187/187. Its re-audit confirmed those findings resolved but exposed a new P1 because global rollback could discard unrelated caller-owned changes. The second corrective RED required a typed clean-context guard; GREEN now rejects an already-dirty context without touching it and reserves rollback for changes begun by this operation. The eleven focal tests and 188/188 overall results from 174 declarations pass; Xcode MCP builds in 8.422 seconds with 0 warnings or Navigator issues. All ten affected files report 0 diagnostics, the existing production container opens with the expanded schema, and static concurrency/API/whitespace checks are clean. The final full-diff re-audit confirmed every P1/P3 resolved and ended without findings. Its only residual risk is that a future Presentation caller must handle `contextHasUncommittedChanges`; no such production mutation exists yet. Firestore, the manually created `develop` client and all remote convergence behavior remain untouched. SwiftUI/accessibility is `N/A`. Subphase 05.6 is complete in `c72917e` and the next gate is the source-backed 05.7 proposal.

- The owner approved one Firebase project with distinct Apple apps. Project `agendapeluqueria-3155f` retains the existing Production app `com.plusprojects.FranAlonso` and now also contains `Fran Alonso Develop` with bundle ID `com.plusprojects.FranAlonso.develop`. During that configuration follow-up, creating the app was the only live Firebase mutation: no Authentication user, Firestore or Storage data, Security Rules or existing Firebase app was read broadly or changed. The later, separately approved provisional Rules deployment is recorded in the current evidence above.
- The project now exposes exactly four configurations: `Debug-Develop`, `Release-Develop`, `Debug-Production` and `Release-Production`. Shared, versioned schemes map Run/Test/Analyze to their Debug configuration and Profile/Archive to their Release configuration. `Develop.xcconfig` and `Production.xcconfig` own the environment name, display name, bundle identifier, primary app icon and local plist path; target-level bundle identifiers and app-icon names were removed so the assigned xcconfig cannot be shadowed.
- The two Firebase plists remain ignored and untracked under `Configuration/Firebase`. `Install Firebase configuration` runs after Resources and before Crashlytics, declares sandbox input/output paths, rejects a missing plist, bundle-ID mismatch or Firebase project-ID mismatch and copies only the selected file to the canonical bundle name. `AppEnvironment` and `CFBundleDisplayName` are expanded from the selected xcconfig. Firebase bootstrap code, App composition and the 05.6 Repository boundary remain unchanged.
- The initial RED added five Swift Testing declarations: the original single configuration lacked `AppEnvironment`, the four-configuration matrix, shared schemes and xcconfig files, producing 4 expected failures while the existing Production plist identity test already passed. The first GREEN passed those 5/5 declarations and 175/175 results in both schemes. The independent audit then found that the build could still accept a plist from a different Firebase project with the same bundle ID. A sixth regression produced the expected focused RED at 0/2 until both xcconfig files declared `FIREBASE_PROJECT_ID` and the build phase compared it with the plist `PROJECT_ID`; that setup closed at 6/6 focused tests and 176/176 results from 162 declarations in both schemes, with its final architecture/data/concurrency re-audit free of findings.
- The subsequent icon regression produced the expected RED because `franalonso-develop.icon` and both xcconfig icon selections were absent while four target-level values still forced `franalonso`. GREEN duplicates the Icon Composer document without altering either PNG or any layer/effect metadata and changes only the automatic-gradient fill from Production `#0088FF` to Develop `#4DB8F5`. The app-icon setting now comes exclusively from the environment xcconfig. Xcode 26.6's native project serialization quotes configuration names and expands xcconfig reference comments; the structural regression follows that canonical output. Under each scheme, the 7/7 focused tests and complete 177/177 results from 163 declarations pass; Xcode MCP builds succeed in 9.588 and 21.544 seconds with 0 warning-level Navigator issues. Compiled `Assets.car` inspection proves Develop contains `franalonso-develop` with the new RGB components and Production contains only unchanged `franalonso`; both emit Default, Dark and Mono renditions. Icon Composer visual inspection confirms all three Develop appearances remain legible and Production retains its original artwork and color. `jq`, `plutil`, exact PNG comparison and `git diff --check` are clean. The final technical and SwiftUI/accessibility audits both closed without findings. Archive/Profile, signing and installation on a physical device remain untested; a final device check should cover Clear/Tinted, large icons and the usual wallpaper because system rendering can reduce the color difference.
- The owner then chose the shorter Develop label `Fran DEV` so identification does not depend on icon color. The existing hosted-app regression first produced the expected RED because Develop still compiled `Fran Alonso Dev`; changing only `APP_DISPLAY_NAME` in `Develop.xcconfig` made that test GREEN while Production continued to require `Fran Alonso`. The 7/7 focused tests and complete 177/177 results pass under both schemes, their Xcode MCP builds complete in 9.143 and 8.710 seconds with 0 warning-level Navigator issues, and the compiled Info.plists prove the respective display name, bundle identifier and environment values. `PRODUCT_NAME`, executable paths, bundle identifiers, icons and Firebase configuration remain unchanged by this follow-up. The fresh final architecture/configuration and SwiftUI/accessibility audits both closed without findings.
- Owner-approved isolation and bootstrap correction 2026-07-25: `FranAlonsoApp` no longer repeats `@MainActor` because SwiftUI's [`App`](https://developer.apple.com/documentation/swiftui/app) protocol supplies that isolation through the primary conformance; the module remains `nonisolated` by default and the rest of the application is not transitively isolated to the main actor. Firebase configuration remains unchanged in the app-delegate launch callback. The phase plan now keeps 05.6 local to the Repository/contextual adapter and defers live Firestore composition to 05.7 with the first `ClientSyncEngine`, after Firebase bootstrap. The mandatory proposal review initially found the unsafe bootstrap order and missing container-first composition; the corrected proposal passed re-review without findings before owner approval. A behavioral RED is `N/A` for this compile-time, behavior-preserving declaration cleanup. Xcode MCP reports 0 file diagnostics, the Develop build succeeds in 17.726 seconds with 0 warnings, and the complete plan passes 177/177 results from 163 declarations; static scope and whitespace checks are clean. The final architecture/data/concurrency audit found one P3 because the baseline described all UI isolation as explicit; after documenting explicit-or-inferred isolation accurately, its focused re-audit closed without findings. SwiftUI/accessibility is `N/A` because no View body, visual resource, preview, localization or accessibility surface changed.
- Subphase 05.5 adds `FirestoreClientRemoteDataSource` inside Clients/Data and keeps every Firebase type inside Data. `FirestoreEnvironment` lives in `Shared/Data/Firebase`, contains only the approved `develop` and `production` values and requires an explicit choice. The Clients adapter owns the feature-specific route and resolves `develop/collections/clients` or `production/collections/clients` without a production default; its live App composition remains deferred to 05.7 after Firebase bootstrap, while 05.6 composes only the local-first Repository boundary.
- Reads use `getDocuments(source: .server)`, decode each snapshot through `data(as: ClientDTO.self)` and reject any document whose route ID differs from the payload `id` with a native `DecodingError.dataCorrupted` at coding path `id`. Upserts address `collection.document(client.id)`, use the full-overwrite Codable `setData(from:completion:)` overload and bridge only that callback boundary with `withCheckedThrowingContinuation`, so success follows the remote acknowledgement rather than local offline queuing.
- Firestore permission-denied and unavailable codes map to neutral transport failures; native cancellation and Firestore cancellation remain `CancellationError`; malformed Codable data preserves its native decoding context; every other provider or foreign error maps to `unexpected`. The exact SDK behavior was checked against Firebase 12.16.0 and its official [collection path](https://firebase.google.com/docs/reference/swift/firebasefirestore/api/reference/Classes/Firestore#collection_), [server query](https://firebase.google.com/docs/reference/swift/firebasefirestore/api/reference/Classes/Query), [Codable write](https://firebase.google.com/docs/reference/swift/firebasefirestore/api/reference/Classes/DocumentReference) and [Codable mapping](https://firebase.google.com/docs/firestore/solutions/swift-codable-data-mapping) documentation.
- The original proposal review found an identity-loss risk and required retaining the Firestore document ID through decoding; its corrected re-review returned no findings. After the owner clarified the new backend paths and approved implementation, a fresh read-only delta review confirmed both odd-segment collection paths, the explicit environment selection and the decision not to compose or access live Firebase in 05.5 without findings.
- RED added the ten adapter test declarations first and the Xcode MCP test build failed on the absent `FirestoreEnvironment` and `FirestoreClientRemoteDataSource` symbols. GREEN passes 11/11 parameterized focal results, 24/24 selected adapter/contract/DTO results and the complete 170/170 plan from 156 declarations. Its original Xcode MCP app build succeeded in 8.827 seconds with 0 warning-level build-log or Navigator issues and both then-new files had 0 diagnostics. After the structural follow-up, 10/10 selected adapter declarations and the complete 170/170 plan pass, including both environment arguments, and the app builds in 8.406 seconds with 0 warning-level log or Navigator issues. The new shared environment and the test file have 0 diagnostics; the isolated adapter refresh retains two contradictory missing-symbol diagnostics despite successful compilation and execution. Static scans find no Firebase imports outside Data in the changed scope, dynamic dictionaries, `JSONSerialization`, unsafe concurrency escapes, prohibited legacy APIs or whitespace defects.
- The manually created `develop` document was deliberately not read or changed. Before the first controlled live access, its document ID and Codable shape must be verified, and an upsert must not run until any non-modelled fields are understood because the approved full-overwrite operation removes omitted fields. Retry/backoff, operational cancellation, rules validation, live smoke testing, Repository composition, sync metadata, conflicts, tombstones and cursors remain outside 05.5. The original final architecture/data/concurrency audit reviewed the complete diff, independently repeated the 170/170 suite and closed without findings. The owner-requested responsibility correction passed both its fresh read-only proposal review and final independent re-audit without findings; the latter classifies the two contradictory isolated adapter diagnostics as stale Source Editor state because build, target membership, tests and Navigator are clean. SwiftUI/accessibility is `N/A`. Subphase 05.5 is published in `d3a0faf` and the next gate is the source-backed 05.6 proposal.
- Subphase 05.4 introduces the provider-neutral `ClientRemoteDataSource: Sendable` contract with server-backed `fetchAll()` and remotely acknowledged `upsert(_:)`, plus `ClientRemoteDataSourceError` for permission, availability and opaque provider failures. The contract accepts only `ClientDTO` values and preserves malformed transport data as native `DecodingError` with its coding path; Firebase types, cache policy implementation, conflict metadata, tombstones, retry classification and cancellation policy remain outside this subphase. The proposal was checked against the phase specification, accepted ADRs, Firebase 12.16.0 source and the official [offline persistence](https://firebase.google.com/docs/firestore/manage-data/enable-offline) and [Swift Codable mapping](https://firebase.google.com/docs/firestore/solutions/swift-codable-data-mapping) guidance. The fresh pre-implementation review found two P2 wording/test-semantics issues: a server read cannot be called conflict-authoritative while latency compensation may merge pending writes, and offline `.unavailable` belongs to the server fetch rather than an immediately failing upsert. Both were incorporated, the re-review returned no findings and the owner approved the corrected scope before code. RED added the five tests first and failed only because `ClientRemoteDataSource` and `ClientRemoteDataSourceError` were absent. GREEN passes 5/5 focal tests, 9/9 affected DTO conversion tests and the complete 159/159 plan from 146 declarations; Xcode MCP builds in 8.249 seconds with 0 warning-level Navigator issues, and both new files report 0 diagnostics after build. Static provider-import, forbidden-API, unsafe-concurrency and whitespace scans plus `git diff --check` are clean. The final independent architecture/data/concurrency audit reviewed the complete diff without findings. It did not repeat the full suite: it reused the recorded 159/159 result and proportionally reconfirmed the 146-test list, build, Navigator warnings and both file diagnostics. Its remaining residual note is that the real Firebase adapter remains deliberately deferred to 05.5. SwiftUI/accessibility is `N/A` because no View, resource, preview, localization or accessibility surface changed. Subphase 05.4 is published in `aad1ead` and the next gate is the source-backed 05.5 proposal.
- Subphase 05.3 adds `ClientPersistenceActor` as the first collection `@ModelActor`. It owns the macro-provided context, delegates fetch/upsert/delete to the existing `ClientLocalDataSource` and crosses isolation only with detached `Client` snapshots or `ClientID`; no live `ClientModel` or `ModelContext` escapes. The fresh pre-implementation reviewer found one P2 missing delete coverage and one P3 missing DocC in the proposal; both were added before the reviewer returned no findings and the owner approved the exact scope. RED changed the tests first and failed at all three call sites because `ClientPersistenceActor` was absent. GREEN passes 3/3 actor tests, the 7/7 affected local DataSource tests and the complete 154/154 plan from 141 declarations. Xcode MCP builds in 9.659 seconds with 0 warning-level Navigator issues. The two isolated Source Editor refreshes return non-blocking error 2 despite successful compilation and execution. Static unsafe-concurrency, forbidden-API and whitespace checks are clean. The final architecture audit found one P3 because two current baselines still named Xcode `17F112` and 138/151 tests; after aligning them to the verified `17F113` and 141/154, the full-diff re-audit returned no findings. SwiftUI/accessibility review is `N/A` because no View, resource, preview, localization or accessibility surface changed. Subphase 05.3 is published in `3ea564f`, remains within active `PLU-11`, and the gate advances to the source-backed 05.4 proposal.
- Owner-requested Data conversion refinement 2026-07-24: the stateless Clients mapper is removed. `ClientDTO` now owns `toDomain()` and `init(_ client:)`; `ClientModel` owns the corresponding detached reconstruction, conversion initializer and context-confined update. `ClientMappingError` preserves all existing structural failures, and Domain receives no knowledge of DTO or SwiftData models. Repository governance applies this as a conditional rule rather than banning mappers: an object remains appropriate when it owns dependencies, configuration, schema versioning, policy or interchangeable strategies. The API Design Guidelines and Swift initialization/extension rules support the chosen conversion initializers and the class-extension convenience initializer. No ADR is needed because the layer direction, source of truth and persistence boundary are unchanged. The mandatory pre-implementation audit approved the design and found one P3 stale phase-03 path, corrected before implementation. RED changed the tests first and failed on the expected absent `ClientMappingError` and `toDomain()` symbols. GREEN passed 16/16 conversion and persistence results; the final complete plan passes 151/151, the final Xcode MCP build succeeds in 5.124 seconds with 0 warning-level log or Navigator issues, and all six affected files report 0 diagnostics. Obsolete-symbol, forbidden-API and whitespace scans plus `git diff --check` are clean. The final architecture audit reviewed the complete diff without findings or residual risks. SwiftUI/accessibility review is `N/A` because no View, resource, preview, localization or accessibility file changed. This follow-up does not advance the 05.3 gate.
- Owner-requested case-less enum namespace cleanup 2026-07-24: repository governance now rejects a project-owned zero-case enum whose only purpose is grouping static members, while preserving documented genuinely uninhabited contracts. The rule chooses by semantic ownership rather than mandating one replacement: `Schema.franAlonso` and `ModelContainer.production`/`inMemory` extend the types they describe or produce; Firebase configuration and the concurrency fixture use free functions at the narrowest workable access; the Clients local data source is an instance service with real context-confined persistence responsibility; and `AppPreviewFixtures.standard` is an immutable fixture value that owns its sample clients and seeding behavior. The inventory therefore moves from seven namespace enums to zero without introducing mechanical empty structs; the remaining 47 enums all model cases. `AGENTS.md`, the development guide, constitution and PR checklist carry the same rule and review gate, with no ADR because this is a repository coding convention rather than an architectural decision. The fresh source-backed pre-implementation review confirmed that direct types, extensions, functions and stateful values were viable and returned no findings. RED changed consumers first and failed on the expected missing `AppPreviewFixtures.standard`; GREEN passes all 6 focal checks, all 21 directly affected results and the complete 151/151 plan. The initial Xcode MCP build passed in 5.42 seconds; the final recheck after documentation and DocC alignment passed in 9.024 seconds, both with 0 warning-level build-log or Navigator issues, and all seven core files report 0 diagnostics. Static namespace/obsolete-call-site scans and `git diff --check` are clean. After one transient SwiftUI hot-reload crash, a fresh session rendered `ClientRow`, `ClientListContent`, and final two-client `ClientListScreen` content at the discovered `Large`, `XXX Large` and `AX 5` variants without clipping or overlap. The final SwiftUI/accessibility audit returned no findings and leaves only a manual VoiceOver traversal as residual evidence. The architecture audit found one P3 from three stale current-state statements in this progress tracker; after reconciling the test count, production schema and 05.6 Repository boundary, its final re-audit returned no findings or residual risks. This follow-up does not advance the 05.3 gate.
- Subphase 05.2 adds `ClientModel` as the first real SwiftData schema, with a unique stable UUID and scalar storage for the optional billing address and consent-backed lifecycle. Its Data-owned extensions keep persistent models inside Data, reuse the 05.1 validation path and reject unknown status values or incomplete persisted addresses. The lightweight `ClientLocalDataSource` instance performs synchronous context-confined fetch, upsert and idempotent delete operations without retaining a context, and its explicit-saving upsert is the single internal write primitive intended for later repository, contextual-adapter and actor composition. `FranAlonsoApp` now installs the production container with `Schema.franAlonso` and `ModelContainer.production(for:)`, handles creation failure explicitly without `try!` or silent fallback and exposes `init(composing:dependencies:)` from a same-file extension for deterministic tests; the live Clients Repository remains the temporary in-memory implementation until 05.6. The App-owned shared preview support installs an in-memory container and preview-only dependencies, while deterministic fixtures seed through the same upsert and safely resurface without duplication. Every existing preview uses the trait and the two previously missing View previews were added. The source-backed proposal and alternatives received a clean independent review and exact owner approval. RED failed on the expected absent model, DataSource, schema, app initializer and preview symbols. GREEN passes 10/10 focused results and the complete plan passes 151/151 from 138 declarations. The corrected Xcode MCP build takes 9.107 seconds with no warning-level log or Navigator issues. The initial 14-file Source Editor pass was clean; after the structural corrections, four isolated refreshes returned non-blocking error 2 even though the build and suites compiled and executed those files. Static scans and `git diff --check` are clean. The supported `Large`, `XXX Large` and `AX 5` renders show the two-client screen without visible clipping, and the shared trait also renders `ContentView` and a representative PDF. The SwiftUI/accessibility audit returned no findings and retained only manual VoiceOver as a residual risk. The architecture audit's P2/P3 were corrected, its evidence P3 was updated and the final full-diff re-audit returned no findings or regressions. Subphase 05.2 is complete and the gate advances to the source-backed 05.3 proposal.
- Subphase 05.1 uses Clients as the first synchronized collection and adds only the neutral `ClientDTO`, nested `BillingAddressDTO`, raw-string `ClientStatusDTO` and bidirectional Data-owned conversion extensions. They canonicalize stable UUIDs, preserve every Client lifecycle/consent invariant and propagate the existing Domain validation error instead of flattening it; JSON decoding retains the native `DecodingError` key and coding path. The source-backed proposal considered manual conversion, synthesized DTO decoding and infrastructure-shaped alternatives; the independent pre-implementation review returned no findings, and the owner approved the exact scope. The original RED failed on the expected absent DTO and conversion symbols. GREEN passed all 9 new fixtures, including valid, missing, wrong-type, unknown-enum, JSON round-trip, lifecycle round-trip and semantic-invalid cases; the 6 affected Client Domain tests also passed, and the complete plan passed 142/142 from 129 enabled declarations. Xcode MCP built successfully in 8.748 seconds with 0 warning-level log or Navigator issues, and all 3 new files reported 0 Source Editor diagnostics. Static scans found no Firebase, SwiftData or SwiftUI import, `JSONSerialization`, unsafe concurrency escape, legacy observation/GCD API, trailing whitespace or `git diff --check` issue. The final independent `$review-ios-standards` audit reviewed the complete diff and returned no findings; it leaves Firestore codec compatibility and SwiftData persistence to their planned subphases. SwiftUI/accessibility review is `N/A` because no View, resource, preview, localization or accessibility surface changed. Subphase 05.1 is complete and the gate advances to 05.2.
- Owner-requested Swift-idiom and conformance cleanup 2026-07-23: the approved scope removes redundant `Equatable` declarations from `BillingDocumentSeries`, `BillingDocumentNumber`, `ClientConsentReference`, `Discount`, `Money` and `TaxRate`, while `Hashable` continues to supply the inherited equality contract. `AppDependencies` now relies on compiler-inferred `Sendable` from its sendable UseCase and actor storage, matching the existing internal-value rule. That pass retained the case-less `DefaultIsolationFixture` and `ModelContainerFactory` enums because Swift permits zero-case enums with type members; the owner's 2026-07-24 repository-style decision above explicitly supersedes that retention and replaces both namespaces semantically. `AGENTS.md`, the development guide, constitution and PR checklist require native Swift constructions, demonstrated abstractions and non-redundant inherited conformances. A new behavioral RED was `N/A` because that original pass was a compile-time, behavior-preserving declaration cleanup; the mandatory source-backed proposal and two independent read-only pre-implementation reviews confirmed its scope and alternatives before owner approval. Xcode MCP built successfully in 15.142 seconds and the complete Swift Testing plan passed 133/133 with no failures or skipped results. Static inventory, whitespace and `git diff --check` checks were clean; SwiftUI/accessibility review was `N/A` because no View, visual resource, preview, localization or accessibility surface changed. The final independent `$review-ios-standards` audit reviewed that complete task scope and returned no findings or residual risks.
- Owner-requested SwiftUI governance refinement 2026-07-23: ADR 0011 supersedes ADR 0001 only where the approved `ModelContext` boundary changes Presentation. The concrete seam is `View → ViewModel → @MainActor closure → Data adapter`: App composes the closure over a Domain value and the ephemeral context, preserving Domain purity and Data ownership of mapping/CRUD/local-first behavior. `$review-ios-standards` now owns architecture, data, concurrency, testing and governance; `$review-swiftui-accessibility` owns declarative View structure, trailing closures, `@ViewBuilder`, `@ScaledMetric`, preview fixtures, Dynamic Type, localization and accessibility. Xcode MCP 26.6 exposed variants from `X Small` through `AX 5`; the current composition preview rendered and was visually inspected at `Large`, `XXX Large` and `AX 5` without truncation. The skill forward test additionally found a pre-existing loading state whose text disappears at AX 5, two Views without `#Preview`, every existing preview without the future shared trait and one redundant `@ViewBuilder`; this is tracked migration debt, not a regression in the documentation-only diff. Apple documentation confirms the PreviewModifier, model-context and scaling semantics. Product RED/GREEN, build and tests are `N/A`; the executable gate remains 05.1.
- Governance validation: the initial architecture audit found the undefined `ModelContext` seam and inaccurate current-state wording; the SwiftUI audit found an obsolete `@Query` allowance, duplicated localization ownership and missing accessibility/localization sources. All valid findings were corrected. Final specialized re-audits and the targeted single-Data-primitive audit returned no P0–P3 findings. The three skills pass `quick_validate.py`; all six root/installable template pairs are byte-identical; 43 repository Markdown files expose 72 local links and 37 kit Markdown files expose 21, with 0 missing targets; 11 ADR index/status rows are consistent; all 18 phase closeouts use the specialized gate; the new skill symlink resolves; and the complete governance diff passes whitespace and scope checks.
- Phase 04 delivery: the complete audited history was fast-forwarded into `main` through `4b74266`, preserving every subphase commit without a merge rewrite. The final executable evidence remains the 133/133 Swift Testing result, 9.163-second Xcode MCP build with 0 warnings and 0 diagnostics across 17 relevant files; the subsequent delivery changes affect only this progress record and the changelog. Git scope and whitespace checks are clean, and phase 05.1 is the next gate.
- Subphase 04.8 defines only the locally materialized observation and local-first save capabilities required by phase 05.10 for Clients, Products, Services and Sales. Each feature owns its Repository protocol plus concrete Observe and Save UseCases; save success means local acceptance, while remote convergence remains a later Data responsibility. The surface deliberately avoids generic CRUD, filtering, deletion, conflict resolution, synchronization and infrastructure-shaped APIs; Billing and Appointments remain deferred to phases 13 and 15. The mandatory pre-implementation `$review-ios-standards` audit reviewed the proposal and alternatives with no findings. RED failed on the expected absent `SaveClientUseCase` and Product/Service/Sale contract symbols. Initial GREEN passed 9/9 focused results and 132/132 overall. The first full-diff audit found one P2 because the temporary in-memory client upsert replacement branch lacked a regression; the added identity-replacement test passes 1/1, and the final complete plan passes 133/133 from 120 enabled declarations. Xcode MCP builds successfully in 9.163 seconds with 0 warnings, and all 17 relevant files report 0 Source Editor diagnostics. Static checks find no forbidden framework or legacy API, strict-concurrency escape, XCTest/XCUITest, trailing whitespace or `git diff --check` issue. The second full-diff audit confirmed the P2 correction and returned no findings. Subphase 04.8 and phase 04 are complete; the gate advances to 05.1.
- Subphase 04.7 adds a pure `Appointment` entity for the deliberately local and reversible phase 15 demo. `Appointment.scheduled` requires one typed client reference, an ordered non-empty set of unique service references and an end instant strictly after the start; private storage prevents synthesized construction from bypassing those invariants. Cancellation is the only mutation: it records a terminal `cancelled` state without changing identity, references or schedule, and a second attempt fails without altering the original cancellation metadata. Custom decoding reimposes schedule and reference validity for both scheduled and cancelled payloads. Overlap policy, catalog activity, daily grouping/calendar, SwiftData and every remote, recurrent, notification, resource or payment concern remain explicitly deferred to phases 15.1–15.3 and 15.6. The mandatory pre-implementation `$review-ios-standards` audit verified the proposal, alternatives, Foundation `Date` semantics and Swift Testing applicability with no findings. Initial RED also exposed a test-only visibility error for a private parameterized argument; after correcting that setup, the accepted RED failed only because `Appointment`, `AppointmentStatus` and `AppointmentError` were absent. GREEN passes 12/12 focused results, including equal/inverted bounds and hostile scheduled/cancelled payloads; the complete plan passes 123/123, and Xcode MCP builds successfully in 9.487 seconds with 0 warning-level build-log or Navigator issues. The isolated Source Editor actions return non-blocking `SourceEditorCallableDiagnosticError error 2` for both files after successful compilation and execution. Static checks find no forbidden framework or legacy API, strict-concurrency escape, XCTest/XCUITest, trailing whitespace or `git diff --check` issue. Both independent full-diff audits confirmed construction paths, inferred `Sendable`, cancellation behavior, Codable defenses, test determinism and scope boundaries with no P0–P3 findings. Subphase 04.7 is complete and the gate advances to 04.8.
- Owner-requested governance refinement 2026-07-23: the repository constitution, execution guide, contributor rules, PR checklist, active Domain wording and future hardening gate now require current primary-source evidence before a technical proposal, a read-only peer review before executable implementation, and exact owner approval before changing known-working code outside an already approved request. Internal structs/enums rely on compiler-inferred `Sendable` only when their contents permit it, actors never repeat conformance, and public/generic boundaries are the documented exception. Project-owned code now prohibits unapproved strict-concurrency escapes (`@preconcurrency`, `@unchecked Sendable`, `nonisolated(unsafe)`), direct GCD/callback-first concurrency, legacy Observation, deprecated APIs and direct Objective-C/legacy choices when a compatible Swift-native route exists. The reusable `$ios-development-standards` and `$review-ios-standards` packages, reviewer rubric and five root/installable project-kit template pairs carry the same gates; the reviewer also supports source-backed pre-implementation proposals. This pass deliberately changes no Swift or project configuration, so RED/GREEN, Xcode build, tests, diagnostics and previews are `N/A`; it does not advance the active executable gate. Cupertino 1.4.2 verified the Swift Evolution and Apple documentation semantics, and all 12 cited primary URLs return HTTP 200. Both skill packages pass `quick_validate.py`; the five template pairs are byte-identical; 42 project Markdown files expose 70 local links and 35 kit Markdown files expose 20, with 0 missing targets; the complete worktree passes `git diff --check`. The pre-existing eight redundant explicit value-type `Sendable` conformances and existing platform/SDK Objective-C or legacy boundaries were inventoried but not changed: under the new approval rule, any cleanup requires a separate concrete proposal and confirmation. A preliminary review corrected the cited-URL count from 11 to 12; both subsequent independent full-scope audits reviewed the corrected project and kit state and returned no P0–P3 findings, so this governance refinement is complete.
- Subphase 04.6 adds a pure immutable `BillingDocument` snapshot with explicit ticket/invoice families, pending and numbered states, and a stable `BillingDocumentRequestID` preserved across both states. `BillingDocumentNumber` accepts only positive values and binds each value to an independent ticket or invoice `BillingDocumentSeries`; the same numeric value may therefore exist in both series without collision. Domain exposes no increment, reservation, counter, Firestore or PDF behavior. Private backing storage plus same-file validating factories/`Decodable` initializers prevent synthesized construction and hostile payloads from pairing a document with the wrong series or restoring a non-positive number. `Sale` continues to reject closure before payment, and every `closed`/`voided` state retains a mandatory `BillingDocumentID`; cross-aggregate verification, atomic remote allocation and replay idempotency remain correctly deferred to phase 13. RED: Xcode MCP discovered 8 new test declarations and the focused action failed to build with the expected missing billing-document, sequence and request-ID symbols. GREEN: all 9 focused results pass, including the two parameterized non-positive values; the complete plan passes 111/111, and Xcode MCP builds successfully in 10.939 seconds with 0 warning-level build-log or Navigator issues. The entity, value object and test file each report 0 live diagnostics. Static checks find no forbidden framework import, `Double`, `JSONSerialization`, `@unchecked Sendable`, XCTest/XCUITest, trailing whitespace or `git diff --check` issue. Both independent full-diff audits confirmed the construction paths, Codable defenses, inferred `Sendable`, stable request identity, independent series and phase 13 boundary with no P0–P3 findings. Subphase 04.6 is complete and the gate advances to 04.7.
- Subphase 04.5 adds a pure `StockWarningPolicy` and transient per-line `StockImpact` projections. Product-linked lines consume the supplied current stock cumulatively in display order, including multiple lines for the same product; professional lines without physical inventory are omitted. Exact depletion to zero is sufficient, while negative projected stock sets `requiresWarning` and returns normally so Domain does not block the sale. Missing stock for a linked product, duplicate line identity and integer overflow fail with typed errors instead of manufacturing a misleading warning. The projections are intentionally non-`Codable` and must be recalculated against current inventory; idempotent stock movements and compensating reversal operations remain deferred to phases 09 and 12. RED: Xcode MCP discovered 9 new declarations and the focused action failed to build with the expected missing `StockWarningPolicy` and `StockWarningPolicyError` symbols. GREEN: all 10 focused results pass, including the two parameterized zero/already-negative inputs; the complete plan passes 102/102, and Xcode MCP builds successfully in 8.486 seconds with 0 warning-level build-log or Navigator issues. The isolated Source Editor diagnostic actions return non-blocking `SourceEditorCallableDiagnosticError error 2` for the policy and test files after successful compilation. Static checks find no forbidden framework import, `Double`, `JSONSerialization`, `@unchecked Sendable`, XCTest/XCUITest, trailing whitespace or `git diff --check` issue. Both independent full-diff audits confirmed the implementation, test surface, struct-construction rules, transient non-`Codable` rationale and phase 09/12 boundary with no P0–P3 findings. Subphase 04.5 is complete and the gate advances to 04.6.
- Subphase 04.4 adds a pure `SaleCalculator` and transient `SaleCalculation`/`SaleLineCalculation` value results without storing derived totals in `Sale`. The active spec and matching DocC make the business rule explicit: catalog and line prices include tax; each line multiplies its snapshot price by quantity, rounds its snapshot discount, extracts taxable base and tax from the discounted total, normalizes every component through `Money`, and only then sums per-line components. Empty sales return zero amounts in an explicit currency, mixed currencies fail with `SaleCalculatorError.incompatibleCurrency`, 100 percent discount remains nonnegative, and mixed tax rates preserve input line identity/order. Global discount remains deferred to 11.7. RED: Xcode MCP discovered the initial 9 new declarations and the focused action failed to build with the expected missing `SaleCalculator`, `SaleCalculation` and `SaleCalculatorError` symbols. Initial GREEN reached 9/9 focused and 91/91 global results. The first independent audit found two valid P2s: synthesized memberwise/`Decodable` routes could manufacture inconsistent derived breakdowns, and tests did not assert the five per-line monetary components. Corrective RED failed with `SaleCalculatorError` lacking `duplicateLineIdentity`. Corrective GREEN makes both result types intentionally non-`Codable`, closes their stored state behind read-only computed properties, uses same-file validating/composition initializers, derives aggregates only from validated line projections, rejects duplicate IDs and directly asserts every per-line component for mixed tax and rounding cases. The corrective duplicate test passes 1/1, all 10 focused results pass, the complete plan passes 92/92, and Xcode MCP builds successfully in 5.403 seconds with 0 warning-level build-log or Navigator issues. The isolated Source Editor diagnostic action returns non-blocking `SourceEditorCallableDiagnosticError error 5` for the policy and test files after successful compilation. Static checks find no forbidden framework import, `Double`, `JSONSerialization`, `@unchecked Sendable`, trailing whitespace or `git diff --check` issue. The second audit confirmed both P2 corrections and found one valid P3 because `calculate(lines:currency:)` did not document its duplicate-identity error; its DocC now names every observable error route. The final corrective full-diff audit returned no P0–P3 findings, so 04.4 is complete and the gate advances to 04.5.
- Owner-requested DocC baseline refinement 2026-07-23: repository governance now requires concise English DocC for semantic production APIs regardless of access level, while explicitly excluding obvious properties, view boilerplate, mechanical `Codable`, trivial private helpers and tests without additional context. The backfill covers the 13 Domain files introduced by 04.1–04.3 and documents type purpose, invariants, units, state transitions, semantic factories, validating initializers, idempotent replays and meaningful parameter/return/error contracts; no `.docc` catalog, dependency, ADR, phase, subphase or executable declaration was added. RED/GREEN is `N/A` because the Swift diff contains only 259 added documentation/blank lines and 0 removed or executable lines. Xcode MCP builds successfully in 6.275 seconds with 0 warning-level build-log or Navigator issues, all 82/82 Swift Testing results pass, and all 13 affected files report 0 live diagnostics. A Swift 6 internal symbol graph for the pure Domain scope converts with `docc --warnings-as-errors`; `git diff --check` is clean, and 42 Markdown files expose 70 local links with 0 broken targets. Both independent full-diff audits returned no findings, so the DocC baseline refinement is complete and the executable gate remains 04.4 `SaleCalculator`.
- Roadmap decision 2026-07-23: the product owner included the Foundation Models assistant in the MVP and deferred GPT-5.6 Luna until after a real-day evaluation, while keeping GPT Realtime only as an option. Accepted ADR 0010 fixes the provider-neutral, typed-draft boundary and forbids background listening, cloud fallback, voice-triggered persistence, and conversational retention. New phase 16 implements and evaluates the stable iOS 26 local stack; former hardening and QA phases move to 17 and 18; phase 19 prepares the Luna adapter behind an authenticated backend. The executable gate remains 04.4 and no Swift/project file changed in this documentation-only planning pass, so RED/GREEN, build, tests, previews, and Xcode diagnostics are `N/A`. Implementer validation: `git diff --check` is clean; 42 Markdown files expose 70 local links with 0 broken targets; all 19 phase specs and 10 indexed ADRs have matching numbers/statuses with 0 consistency errors. Linear reconciliation created [PLU-9](https://linear.app/plusprojects/issue/PLU-9/implement-phase-16-on-device-voice-assistant) and [PLU-10](https://linear.app/plusprojects/issue/PLU-10/evaluate-and-integrate-gpt-56-luna-after-mvp) in Backlog, verified PLU-10 is blocked by PLU-9, refreshed the project and active Phase 04 milestone to gate 04.4, and left PLU-8 In Progress without creating a future branch. The Obsidian dashboard and development log were read back successfully with the same gate and roadmap. The first independent documentation audit found one valid P3 because this evidence had not yet been recorded here; after this correction, the second full-diff audit confirmed the P3 resolved and returned no P0–P3 findings or regressions.
- Subphase 04.3 implements the pure sale aggregate as an explicit finite-state lifecycle: `draft → inProgress → awaitingPayment → awaitingDocument → closed`, followed only by the compensating `voided` transition. `SaleLine` preserves the service ID/name, quantity, unit price, tax, discount and optional product link as an immutable historical snapshot, while its state advances only `upcoming → inProgress → completed`. `PaymentID` and `SaleReversalID` provide stable operation identity; repeating equal payment, document-close or reversal payloads is a no-op, while reusing an operation after changing its payload produces a typed conflict. Payment and later metadata transitions preserve the line array exactly. The public construction API uses named `draft`/`upcoming` factories because they express initial domain state; no assignment-only initializer was added, and the validating private plus `Decodable` initializers live in the same-file extensions and reimpose quantity, line-identity and aggregate state consistency. RED: Xcode MCP discovered the initial 16 new declarations and the focused action failed to build with the expected missing `Sale`, `SaleLine`, `SaleStatus` and `SaleLineStatus` types. Initial GREEN reached 17/17 focused and 80/80 global results. The first independent audit found a valid P2 because duplicate line identity was enforced but untested through both construction routes, and a valid P3 because a generic private `make` factory contradicted the construction-API rule. Two corrective tests now prove `Sale.draft` and adversarial decoding reject duplicate IDs, while `SaleLine` reuses a private throwing initializer for validation and retains only the semantically named public `upcoming` factory. The corrective tests pass 2/2, all 19 focused results pass, the complete plan passes 82/82, and Xcode MCP builds successfully in 4.238 seconds with 0 warning-level build-log or Navigator issues. `Sale`, `SaleLine`, `SaleStatus` and their test file each report 0 live diagnostics; static scope checks find no forbidden framework import, `Double`, `JSONSerialization` or `@unchecked Sendable`, there is no trailing whitespace, and `git diff --check` is clean. The second independent full-diff audit confirmed both corrections, found no P0–P3 regressions and left only later stock/financial compensation work in its specified future subphases. Subphase 04.3 is complete and the gate advances to 04.4.
- Subphase 04.2 construction-API refinement applies the owner's clarified style without weakening Domain invariants. RED: the focused Swift Testing build failed with `Type 'Client' has no member 'draft'` after the expected named factory was written first. GREEN: the assignment/default-only `Client(id:displayName:)` convenience initializer became `Client.draft(id:displayName:)`, and every fixture and preview now states that domain intent explicitly. `Money`, `TaxRate`, `Discount`, `ClientConsentReference` and `Service` keep their throwing validation plus required `Decodable` initializers; `ObserveClientsUseCase`, `ClientListScreen` and `AppDependencies` keep meaningful dependency-injection/composition initializers. Repository governance and both reusable iOS skills now require reviewers to prefer the memberwise initializer for passive data, accept a named factory only when it adds state/preset/composition meaning, retain meaningful initializers in same-file extensions, and reject post-construction validation that permits invalid state. The focused factory test passes 1/1, the complete plan passes 63/63, and Xcode MCP builds successfully in 4.680 seconds with 0 warning-level build-log or Navigator issues. Five representative production/test files report 0 live diagnostics, `git diff --check` is clean, and both skill packages pass `quick_validate.py`. The first independent audit returned no findings. The second full-diff audit found one valid P2 because this progress entry still described both audits as pending; the state and next actions were corrected, and the corrective full-diff re-audit confirmed the P2 resolved with no further findings. This refinement is complete and the gate returns to 04.3.
- Subphase 04.2 expands the pure Domain boundary with billing-aware `Client`, `BillingAddress`, consent activation states, price-free inventory `Product`, and commercial `Service`. `Client` defaults to `draft`; `active` carries a validated non-empty `ClientConsentReference`, so synthesized Client/Status decoding must also pass through that value-object validation. `Product` exposes only typed identity, name and active/inactive state, with no price or discount. `Service` carries `Money`, `TaxRate`, optional `Discount` and status; its throwing initializer requires `linkedProductID` for product services and rejects it for professional services, and custom decoding routes through the same initializer. The active phase spec records these exact 04.2 policies and remains consistent with ADR 0009 and phases 08–10; no new ADR is required. RED: Xcode MCP discovered the original 9 new declarations and the focused action failed to build with the expected missing-type/member errors before any 04.2 production implementation existed. Initial GREEN: all 9 focused tests pass, including Codable round trips, inferred `Sendable` compile guards, the price-free Product construction surface, both valid service variants, both invalid links and invalid Service decoding. The initial complete plan passed 61/61, and Xcode MCP built successfully in 8.284 seconds with 0 warning-level build-log or Navigator issues. The first independent audit reported two P2s. Its missing-regression finding was valid: a round trip now covers `consentPendingUpload`, and an adversarial synthesized-enum payload proves decoding cannot create `active` with an empty consent reference. Its proposed fourth inactive Client state was not adopted because accepted ADR 0009 defines exactly `draft`, `consentPendingUpload` and `active`, while accepted ADR 0006 assigns client removal to the synchronization tombstone; phase 08 now makes that distinction explicit and states that deactivation preserves the consent reference in the retained payload. The two corrective tests pass 2/2, the complete plan passes 63/63, and Xcode MCP builds successfully in 7.712 seconds. Static scope checks find no `Double`, `JSONSerialization`, `@unchecked Sendable`, or SwiftUI/SwiftData/Firebase/UIKit import in the affected Domain code, and `git diff --check` is clean. The second independent full-diff audit confirmed both first-pass resolutions and returned no P0–P3 findings. An owner-requested follow-up then removed the unnecessary `countryCode` from `BillingAddress` and established a cross-project struct-initializer rule. Follow-up RED: the focused client-domain test failed to compile with `Missing argument for parameter 'countryCode' in call` after the expected API was written first. Follow-up GREEN: redundant assignment-only initializers were removed from all typed IDs; custom struct initializers in Domain, App composition and the SwiftUI Screen moved to same-file extensions; validated value objects and `Service` use private backing storage so synthesized construction cannot bypass their invariants. The focused test passes 1/1, the complete plan passes 63/63, Xcode MCP builds successfully in 4.965 seconds, and build-log plus Navigator warnings remain at 0. Eleven affected production files report 0 live diagnostics; the isolated test-file refresh returns non-blocking `SourceEditorCallableDiagnosticError error 5`. Repository governance and the reusable implementation/reviewer skills now require this check; both skill packages pass `quick_validate.py`. Both independent follow-up audits inspected all 42 production/test Swift files, confirmed the rule, stable Codable keys and preserved APIs, and returned no findings. The 04.2 follow-up is complete and the gate returns to 04.3.
- Subphase 04.1 starts the shared pure Domain model with `Money`, `TaxRate`, `Discount`, `Currency` and seven UUID-backed entity-specific identifiers for the entities named by phase 04. `Client` now exposes `ClientID` instead of a naked UUID, and every affected deterministic fixture and preview uses that typed boundary. `Money` stores `Decimal`, rounds to the currency minor-unit scale with Foundation `.plain`, rejects `NaN` and incompatible-currency addition, and revalidates decoded values. `TaxRate` and `Discount` accept only the closed decimal range 0...100, reject `NaN`, and route `Decodable` through the same validating initializer so serialization cannot manufacture an invalid value. The active phase spec records the rounding, currency-mismatch and exact percentage-range policies; no ADR is needed because these refine the executable domain contract without changing an accepted architecture decision. RED: Xcode MCP discovered the original 15 new test declarations and the focused run failed to compile because `Money`, the percentage types/errors and typed IDs did not exist. Initial GREEN: those declarations expand to 21/21 passing results, including range boundaries, rounding, invalid decoding, currency mismatch, Codable identity and the typed `Client` boundary. The first independent audit found one P2 and three P3s: the Codable test did not prove that raw unnormalized Money decoding re-enters validation, the spec omitted the exact percentage interval/`NaN` rule, the suite counts were stale and the blocker wording still referred to starting 04.1. All four findings were incorporated. The new direct-decoding regression proves `10.005 EUR` materializes as `10.01 EUR`; its six focused Money tests pass 6/6. Corrective full validation passes 52/52, and Xcode MCP builds successfully in 4.225 seconds with 0 warning-level build-log or Navigator issues. Static scope checks find no `Double`, `JSONSerialization`, `@unchecked Sendable`, or SwiftUI/SwiftData/Firebase/UIKit import in the affected Domain code, and `git diff --check` is clean. Per-file Source Editor refresh returned non-blocking `SourceEditorCallableDiagnosticError error 2` for the six primary files even though all compile in the successful complete test/build actions. The second independent full-diff audit confirmed all four findings resolved and returned no new findings. Subphase 04.1 is complete and the gate advances to 04.2.
- Subphase 03.6 verifies the architecture review contract instead of duplicating checks already installed by the project kit. `AGENTS.md`, the PR checklist and ADR 0001 already require Domain/Data/Presentation boundaries and explicit responsibility suffixes; `$review-ios-standards` independently requires layer-direction and naming review. The controlled RED fixture added one temporary untracked production file at `Features/Clients/Domain/Entities/ClientManager.swift` containing a SwiftUI import and an ambiguous empty `Manager`, then sent only that diff to a fresh context-free read-only reviewer. The reviewer reported the expected boundary P2 and naming P3 with exact lines and remediation, proving both checks execute rather than merely exist on paper. GREEN removed the fixture completely; the only remaining worktree change is this `Progress.md` evidence. Repository scans found no prohibited UI/Data/Firebase imports in feature Domain files, no executable/test type with an ambiguous `Interactor`, `ModelLogic`, `Manager`, `Helper`, `Utils` or `Common` suffix, and no empty `Stores` directory. No review-rule duplication, application behavior or project configuration remains. RED/GREEN Swift tests, Xcode build and previews are `N/A` because the final subphase is verification/documentation-only; the temporary fixture was the review acceptance test and was never intended for the target. The first closeout audit found one valid P3 because the initial evidence called the worktree clean despite this documentation diff; the wording was corrected and proportional validation remained clean. The second full-diff audit confirmed that P3 resolved and returned no findings. Subphase 03.6 and phase 03 are complete; the gate advances to 04.1.
- Subphase 03.5 documents an operational Store extraction gate in the active architecture spec. Extraction now requires a cohesive independently nameable capability, owned state/tasks/transitions, preservation of the ViewModel as screen facade, Domain-only dependencies and at least one demonstrated pressure such as reuse, an independent lifecycle or a separately valuable state machine. Line count, hypothetical reuse and wrapping one UseCase are explicitly insufficient. The ownership example keeps composition in `App`, creation in the `Screen`/ViewModel boundary and Store construction and retention in the ViewModel. Its `ClientEditingStore` owns the draft plus editing/save/error phases and structured save, retry and cancellation transitions; the `ClientDetailViewModel` separately coordinates successful editing toward navigation and exposes Store tracking without mutable state duplication. The current `ClientListViewModel` still owns only one `ObserveClientsUseCase` state machine, so no ceremonial `ClientListStore`, empty folder or executable code was added. RED/GREEN, tests, build and previews are `N/A` under the development guide and ADR 0003 because this is documentation-only. Proportional and corrective validation checked 39 Markdown files and 65 local links with 0 broken targets, confirmed that the diff contains no executable/project-configuration file, found no production/test Store type and passed `git diff --check`. The first independent audit found a valid P2 because the initial example claimed editing/retry/cancellation ownership while only wrapping one UseCase; the expanded example resolved it. The second full-diff audit confirmed that P2 resolved and found two P3 findings: stale immediate actions and a claim that the example included validation it did not model. Both were corrected, and the final corrective full-diff audit confirmed the P2 and both P3 findings resolved with no new findings. Subphase 03.5 is complete and the gate advances to 03.6.
- Subphase 03.4 replaces the bootstrap placeholder with a functional declarative Clients vertical. `ContentView` resolves the typed dependency container only at the composition boundary; `ClientListScreen` receives `ObserveClientsUseCase`, creates and retains the `@Observable @MainActor` ViewModel, and invokes structured `load()` work from lifecycle-bound `.task`. `ClientListContent` renders loading, empty, content and error states, while `ClientRow` uses stable `Client.id` identity. Six semantic Spanish String Catalog keys cover every new visible UI string. No Store was introduced because the vertical still has one cohesive responsibility, and the native UI-test case is explicitly `N/A` under spec 03.4 and ADR 0003; state behavior remains covered in the ViewModel. The preflight found that the `@Entry` default constructed fresh dependency graphs on separate `EnvironmentValues` reads. RED: the new identity test failed because the two default telemetry reporters differed. GREEN: one private stable preview default satisfies that contract without adding a singleton or exposing Data to SwiftUI. The seven focused tests pass 7/7, the initial complete Swift Testing plan passes 29/29, and Xcode MCP builds in 7.882 seconds with 0 warning-level build-log or Navigator issues. All five affected production Swift files report 0 live diagnostics; the isolated test-file refresh returns `SourceEditor error 5`, but that file compiles and executes in the focused and full-plan runs. Xcode MCP renders all four deterministic state previews, an empty-state AX5 variant without clipping and the full composition preview. Static checks confirm a valid manual Spanish catalog, no forbidden imports or names, no XCTest/XCUITest, `JSONSerialization`, `@unchecked Sendable` or unstructured production task, no `project.pbxproj` change, an ignored Firebase plist and a clean `git diff --check`. Both independent full-scope `$review-ios-standards` audits returned no findings. An owner-requested follow-up establishes that base value and Domain models conform to `Identifiable`, `Codable` and `Equatable` whenever semantically possible, while internal model structs rely on inferred `Sendable` when their stored properties permit it. `Client` now implements those three explicit conformances, and `List(clients)` consumes its stable immutable identity. Follow-up RED: the new model test failed to compile because `Client` lacked `Encodable` and `Decodable`; GREEN: its JSON round trip preserves equality and identity, the focused test passes 1/1, the complete plan passes 30/30, Xcode MCP builds in 8.512 seconds with 0 warnings, and the content preview renders unchanged. `Client.swift` and `ClientListContent.swift` report 0 diagnostics; the test-file refresh returns the non-blocking `SourceEditor error 5`. The follow-up review sequence identified and resolved two P3 findings: stale 27/29 progress counts and redundant explicit `Sendable` declarations on two private test enums. Final corrective validation passes the model plus six ViewModel tests 7/7, the complete plan 30/30, and Xcode MCP builds in 7.771 seconds with 0 warning-level build-log or Navigator issues; the corrected test file reports 0 diagnostics. Subphase 03.4 remains complete and the gate remains 03.5.
- Subphase 03.3 adds the required `ClientListViewModel` as a final `@Observable @MainActor` screen facade under Clients/Presentation. It receives only `ObserveClientsUseCase` by initializer and maps observation into explicit `idle`, `loading`, `empty`, `content` and `failed` visual states. `load()` remains structured `async` work with no internally created task; cancellation returns to `idle`, so the Screen can own its lifetime through `.task` in 03.4. No Store was introduced because this vertical still has one cohesive responsibility. Initial RED: Xcode MCP discovered all five original tests but rejected their build because `ClientListViewModel` and its states did not exist. Initial GREEN: the five focused tests passed 5/5, the complete plan passed 27/27 and Xcode MCP built in 7.723 seconds with 0 warning-level build-log or Navigator issues. The first independent audit found one valid P2: a stream ending normally without any snapshot left the state at `loading`. A deterministic `.finished` fake reproduced the corrective RED with the observed state `.loading` instead of `.empty`; tracking whether a snapshot arrived and resolving normal no-emission completion after `Task.checkCancellation()` fixed the transition without converting cancellation into empty. Final GREEN: the six focused tests pass 6/6, the complete plan passes 28/28 and Xcode MCP builds in 4.285 seconds with 0 warning-level build-log or Navigator issues. `ClientListViewModel.swift` reports 0 live diagnostics; the isolated test-file refresh returns `SourceEditor error 5`, but the file compiles and all six tests execute in both focused and full-plan runs. Static checks find no forbidden imports, names, XCTest/XCUITest, `JSONSerialization`, `@unchecked Sendable` or unstructured production task, and `git diff --check` is clean. The second independent full-diff `$review-ios-standards` audit confirmed the P2 resolved and returned no findings. Subphase 03.3 is complete and the gate advances to 03.4.
- Subphase 03.2 composes the first feature repository without leaking it to SwiftUI. `InMemoryClientRepository` is an actor under Clients/Data that implements the Domain-owned `ClientRepository` contract with a deterministic finite snapshot; it deliberately contains no SwiftData or Firebase behavior. `AppDependencies` requires the repository explicitly, constructs `ObserveClientsUseCase`, exposes that use case rather than the repository, and keeps `AppEnvironment` as the existing single typed `@Entry`. `live()` uses an empty in-memory implementation only as the current architecture-phase backend; `preview(clients:)` accepts deterministic seed data. RED: the focused Xcode MCP test build failed with `Extra argument 'clientRepository' in call` and `Value of type 'AppDependencies' has no member 'observeClients'`. GREEN: the two focused composition tests pass 2/2, the complete plan passes 22/22, and Xcode MCP builds successfully in 7.327 seconds with 0 warning-level build-log or Navigator issues. `InMemoryClientRepository.swift` and `AppDependenciesTests.swift` report 0 live diagnostics. The isolated Source Editor refresh for `AppDependencies.swift` retains two stale `Cannot find 'InMemoryClientRepository' in scope` errors despite the successful compiler build and tests that execute the injected and preview composition paths; they are recorded as an indexing limitation rather than executable evidence. Static checks find no forbidden imports, names, XCTest/XCUITest, `JSONSerialization` or `@unchecked Sendable`; `git diff --check` is clean. Both independent `$review-ios-standards` audits returned no findings and confirmed composition, Data placement, typed Environment distribution, concurrency and test coverage. Subphase 03.2 is complete and the gate advances to 03.3.
- Subphase 03.1 proves the first Domain-directed vertical without introducing Data or Presentation. `Client` is a minimal immutable `Equatable` and `Sendable` entity; `ClientRepository` owns the asynchronous observation contract in Domain; and `ObserveClientsUseCase` depends only on that contract and forwards its stream. The repository fake remains private to the Swift Testing file and is an actor, so call tracking is deterministic under complete strict concurrency. RED: Xcode MCP rejected the focused test build exclusively because `Client`, `ClientRepository` and `ObserveClientsUseCase` did not exist. GREEN: the focused test passes 1/1, the complete plan passes 20/20, and Xcode MCP builds successfully in 7.816 seconds with 0 warning-level build-log or Navigator issues. All three production files report 0 live diagnostics; the isolated test-file diagnostic action returned `SourceEditor error 5`, but that file compiled and passed in both focused and full-plan execution. Static boundary checks find no SwiftUI, SwiftData, Firebase or UIKit imports in Domain, no forbidden names, and no XCTest, XCUITest, `JSONSerialization` or `@unchecked Sendable` in the new scope. `git diff --check` is clean. Both independent `$review-ios-standards` audits returned no findings and retained only the non-blocking Source Editor diagnostic limitation. Subphase 03.1 is complete and the gate advances to 03.2.
- Subphase 02.10 is documentation-only, so RED/GREEN, build and test execution are `N/A`: no executable source or project configuration changed in this subphase, and the latest executable baseline remains the 19/19 Swift Testing plan plus the warning-free Xcode MCP build recorded by 02.9. Implementer verification inspected `AGENTS.md`, the development guide, constitution, active bootstrap spec, all accepted ADRs, the PR checklist, phase index, README, changelog, progress and the next phase boundary. A local-link validator checked 39 Markdown files and 65 local links with 0 broken targets; all 7 required governance documents exist, ADR 0001–0009 are 9/9 `Aceptado` and 9/9 indexed, all 18 phase/index specs exist, and the governance sources contain 0 unresolved placeholder tokens. The inspection also confirms 17 Swift Testing declarations, no XCTest/XCUITest, `JSONSerialization` or `@unchecked Sendable` in executable/test sources, the shared `Package.resolved` tracked, and `GoogleService-Info.plist` ignored. Two inconsistencies were corrected: the stale README now reflects the completed constitution/bootstrap, and the PR checklist now permits only `ModelContainer` composition in App while keeping SwiftData models/persistence in Data; the changelog also records the accumulated 02.7–02.10 bootstrap work. `git diff --check` is clean. Both independent full-diff `$review-ios-standards` audits returned no findings and confirmed the documentation-only `N/A`, phase/gate wording, link/state evidence and architectural checklist correction. Subphase 02.10 and phase 02 are complete; the gate advances to 03.1.
- Subphase 02.9 uses stable resource names `client-consent-template`, `billing-ticket-a4-template` and `billing-invoice-a4-template`. The supplied legacy assets and signature/stamp photograph were inspected without modifying their source; the real signature was deliberately excluded from Git and the bundle. RED: the two focused Swift Testing declarations failed to build because `DocumentTemplateResource` did not exist. GREEN: the loader and three generated single-page A4 PDF files satisfy the exact-name, page-count and media-box assertions (4/4 expanded results). The source JSON records only version, structure and confirmed non-localizable business identity/data; all localizable document copy now lives in the dedicated `DocumentTemplates.xcstrings` catalog with 68 semantic keys, Spanish values and translator comments. The provisional vector wordmark and PDF author metadata derive from the single `tradeName` identity value instead of duplicating its words, and the tax-identifier label comes from the catalog. The invariant generator produced identical SHA-256 hashes across consecutive runs in the validated CPython 3.12.13/ReportLab 4.4.9 environment. `pdfplumber` confirms the expected legal, address and signature-area text in all PDFs. Poppler renders at 144 dpi showed no clipping, overlap or unreadable content, and all three PDFKit previews rendered successfully through Xcode MCP. The complete plan passes 19/19; Xcode MCP builds in 4.698 seconds with zero warning-level build-log or Navigator issues, both new Swift files report zero diagnostics, and the build log records all three PDFs copied into the app. The first audit found one valid P2 because regeneration originally omitted the required ReportLab environment. The fix pins ReportLab 4.4.9, Pillow 12.2.0 and charset-normalizer 3.4.9, documents CPython 3.12.13/zlib 1.2.12 and scopes the binary-stability claim; a newly created isolated venv installed only those requirements, regenerated all three PDFs and reproduced every committed SHA-256 exactly. The next audit found a valid P3 because localizable copy was split between JSON and generator; the dedicated catalog and localization-guide exception resolved it. Subsequent corrected full-diff audits found that the wordmark words, `NIF` label and PDF author metadata still had independent generator literals. Those P3 findings were fixed without changing the PDF output: isolated regeneration again reproduced every committed hash, the catalog validates 68/68 entries, and all brand identity now resolves from `tradeName`. The final corrected full-diff `$review-ios-standards` re-audit returned no findings; it retained only the explicit legal-approval, definitive-wordmark and locked-generator-environment risks. Subphase 02.9 is complete and the gate advances to 02.10.
- Subphase 02.8 good-practice preflight used the repository standards, dependency-injection guidance, and Apple SwiftUI documentation from the installed Xcode 26.6 SDK. Cupertino MCP remained unavailable with `Transport closed`; Xcode documentation confirmed `@Entry` as the modern custom `EnvironmentValues` API and root `.environment` propagation. The implementation composes only the real shared capability that already exists: `AppDependencies` builds `TelemetryReporter` from its app-owned data-source contracts, `live()` selects the Firebase adapters, and `preview()` selects private deterministic no-op adapters. `AppEnvironment.swift` is deliberately only the environment entry rather than a second wrapper, and the first repository composition remains in phase 03.2 because no repository contract exists yet. RED: the two focused tests failed to build with `Cannot find 'AppDependencies' in scope` and `EnvironmentValues` missing `appDependencies`. GREEN: the composition test proves injected spies receive the denied/granted collection sequence and technical events through the shared reporter; the environment test proves the exact container round-trips through `EnvironmentValues`. The focused tests pass 2/2, the complete plan passes 15/15, and Xcode MCP builds successfully in 7.082 seconds with 0 warning-level build-log or Navigator issues. All three affected production files report 0 live diagnostics; the isolated Source Editor diagnostic action is unavailable for the new test file, but that target compiled and both tests executed successfully. The first independent audit found two valid P2s. Official Firebase Crashlytics documentation confirmed that a runtime collection override persists but does not take effect until the next run, so the review finding is the RED evidence for removing the misleading root `.task`; the app now performs no current-launch collection mutation, and no artificial Firebase-real test was added for SDK lifecycle semantics that deterministic spies cannot reproduce. The active 02.8 spec now asks for deterministic doubles of real existing capabilities and explicitly reserves the first repository fake for 03.1/03.2, avoiding a ceremonial contract while keeping the spec as source of truth. After both corrections, the focused tests pass 2/2, the complete plan passes 15/15, and Xcode MCP builds successfully in 3.884 seconds with 0 warnings, Navigator issues, or `FranAlonsoApp.swift` diagnostics. The second independent full-diff audit confirmed both P2s resolved and returned no findings; it retained only the documented future Crashlytics-consent lifecycle and unavailable Cupertino transport as residual risks. Subphase 02.8 is complete and the gate advances to 02.9.
- Subphase 02.7 good-practice preflight used the repository standards and Apple SwiftData documentation from the installed Xcode 26.6 SDK. Cupertino MCP 1.4.2 and all local source databases are installed, but its MCP transport returned `Transport closed`; Xcode documentation supplied the API evidence instead. Apple documents `ModelConfiguration.isStoredInMemoryOnly` for ephemeral stores, `ModelContext.save()` for explicit pending insert/update/delete persistence, `.none` to disable managed CloudKit and App Group discovery, and `@ModelActor` for isolated model-context access. The implementation therefore avoids a dummy production model, accepts an explicit `Schema`, disables CloudKit/App Group storage, and defers the first real `@ModelActor` to phase 05.3 rather than creating a ceremonial actor. RED: the focused Swift Testing build failed because `ModelContainerFactory` was absent (`Cannot find 'ModelContainerFactory' in scope`). GREEN: `ModelContainerFactory.makeProduction` and `makeInMemory` share one minimal container construction with optional migration-plan support; the test-only `PersistenceFixtureModel` performs insert, update, and delete through fresh contexts, proves unsaved data is invisible to another context, and calls `save()` only with `hasChanges`. The focused test passes 1/1, the complete plan passes 13/13 in parallel, and Xcode MCP builds successfully in 7.110 seconds with 0 warning-level build-log or Navigator issues; both affected files report 0 live diagnostics. Both independent full-diff audits returned no findings and confirmed that the production factory is complete at this boundary while persistent-disk execution, SwiftUI attachment, the first business `@Model`, and `@ModelActor` correctly remain deferred until their real schema and composition exist. Subphase 02.7 is complete and the gate advances to 02.8.
- Subphase 02.6 direct-call follow-up is a behavior-preserving simplification, so a new artificial RED is `N/A`: retaining an injectable lifecycle test would preserve the production seam being removed, while exercising `FirebaseApp.configure()` in a unit test would violate the deterministic-Firebase rule. `AppDelegate` now calls `FirebaseConfigurator.configure()` directly from `application(_:didFinishLaunchingWithOptions:)`; its stored closure and both custom initializers were removed, together with the lifecycle test and its now-unused UIKit import. The two remaining Firebase configuration tests pass 2/2, the complete Swift Testing plan passes 12/12, and Xcode MCP builds successfully in 4.408 seconds with 0 warning-level build-log or Navigator issues. `AppDelegate.swift` has 0 live diagnostics; Xcode's isolated Source Editor diagnostic action is unavailable for the test file, but its target compiled and executed successfully. Both independent full-diff audits returned no findings and reconfirmed the direct composition, Firebase isolation, deterministic remaining tests, resource integrity, Crashlytics configuration, and local plist privacy. The intentional residual is that this one-line lifecycle wiring is covered by inspection and compilation rather than a Firebase-real unit test. Subphase 02.6 is complete and the gate returns to 02.7.
- Subphase 02.6 final organization is a structural refactor with no new behavior, so a new artificial RED is `N/A`; the existing lifecycle/privacy tests and build/resource pipeline are the proportional regression proof. `AppDelegate` moved unchanged to `App/AppDelegate.swift`; `Assets.xcassets`, `franalonso.icon`, and the local `GoogleService-Info.plist` moved under `Resources` beside `Localizable.xcstrings`. Xcode initially staged the relocated synchronized resources, including the ignored plist; the implementer immediately removed those index entries, and the final plist is ignored by `**/GoogleService-Info.plist`, untracked, and unstaged. The full Swift Testing plan passes 13/13 and Xcode MCP builds successfully in 7.357 seconds with 0 warning-level build-log or Navigator issues. The build log proves `actool` reads both resource paths with `--app-icon franalonso`, emits `Assets.car`, copies `Resources/GoogleService-Info.plist` into the app bundle, and executes the Crashlytics dSYM phase using that packaged plist; source and packaged Firebase bundle IDs match. `AppDelegate.swift` reports 0 live diagnostics. The isolated refresh for `FranAlonsoApp.swift` retains a stale cross-file `Cannot find 'AppDelegate' in scope` diagnostic despite the successful compiler build, passing tests, clean build log, and clean user-visible Issue Navigator; it is recorded as an Xcode source-editor indexing artifact rather than build evidence. Both independent full-diff audits returned no findings and reconfirmed resource identity, target membership, plist privacy, clean Git index, and the Xcode MCP validation evidence. Subphase 02.6 is complete and the gate advances to 02.7.
- Subphase 02.6 simplification RED: `FirebaseBootstrapTests` required the new `AppDelegate(configureFirebase:)` closure initializer before it existed, and the targeted test build failed with `Argument passed to call that takes no arguments`. GREEN: `AppDelegate` now invokes a test-injectable launch closure whose production value is `FirebaseConfigurator.configure`; the concrete configurator retains the idempotency and local-plist guard inside `Bootstrap/Data/Infrastructure/Firebase`. The forwarding-only `ApplicationBootstrap`, one-method `BackendConfigurationDataSource`, its fake, the empty `Bootstrap/Domain` layer, and the pass-through `AppRoot` view were removed; `FranAlonsoApp` is the composition root and presents `ContentView` directly. The duplicate bootstrap test was removed and `ProjectSanityTests` now instantiates the actual app composition root. The focused test passes 1/1, the complete Swift Testing plan passes 13/13, and Xcode MCP builds successfully in 7.429 seconds with 0 warning-level build-log, Navigator, or affected-file diagnostic issues. The first independent audit found one P2 because Immediate Next Actions skipped directly to 02.7 while reviews were still open; the action list was corrected and the renamed focused test passed 1/1. The second full-diff audit confirmed the P2 resolved and returned no findings, so 02.6 is complete and 02.7 is the next gate.
- Subphase 02.6 lifecycle adjustment RED 1: `FirebaseBootstrapTests` could not compile because `AppDelegate` did not exist. GREEN 1 initially registered an injected `UIApplicationDelegate` adapter whose launch callback started `ApplicationBootstrap`; the subsequent simplification recorded above removed that forwarding layer while preserving the tested lifecycle behavior. RED 2: the versioned Info.plist did not contain `FirebaseAppDelegateProxyEnabled`; GREEN 2: the proxy is explicitly disabled and regression-covered so future Messaging/APNs integration cannot silently depend on swizzling. No Messaging SDK product, notification permission, push entitlement, or background mode was added. That intermediate implementation passed 14/14 tests; Xcode MCP built successfully in 7.171 seconds with 0 warning-level build-log or Navigator issues, and live diagnostics for the app entry point reported 0 issues. Its first independent audit found one P2 documentation-timing issue because 02.6 had been marked complete before both new reviews finished; `Progress.md` was corrected to keep the gate in review. The second full-diff audit confirmed that fix and returned no findings.
- Subphase 02.6 RED: the initial Swift Testing suites could not compile because `ApplicationBootstrap`, `BackendConfigurationDataSource`, the telemetry contracts/models, and `TelemetryReporter` did not exist. GREEN: the application now configures FirebaseCore through an injected app-owned boundary; Analytics and Crashlytics use separate async `Sendable` contracts, a closed technical allowlist, explicit consent, and failure-tolerant reporting without real Firebase in tests. The first independent audit found that enabling Firebase Analytics would also enable SDK-managed automatic events outside that allowlist. The valid P2 was fixed without weakening ADR 0007: Firebase Analytics is permanently deactivated, automatic screen reporting and IDFV collection are disabled, and its concrete adapter fails closed; a controlled configuration regression reproduced RED with the two required keys absent and GREEN after restoration. The second audit then reproduced an actor-reentrancy race where an obsolete grant could reactivate Crashlytics after revocation. A suspendible deterministic fake reproduced RED at 11/12; revision-based `latest-wins` reconciliation now compensates stale sink writes and the full plan passes 12/12. Firebase 12.16.0 and the six approved direct products are exact and the shared `Package.resolved` is tracked; IdentitySupport/AdSupport are absent, Firebase imports occur only in `Data/Infrastructure/Firebase`, and the local plist is ignored/untracked without exposing its contents. Xcode MCP builds in 3.893 seconds with 0 warning-level build-log or Navigator issues; its log confirms `-ObjC`, generation of both `FranAlonso.app.dSYM` binaries, execution of the final `Upload Crashlytics dSYMs` phase, and successful synchronous `upload-symbols` validation. The packaged executable and debug-dylib dSYM UUIDs match. The final full-diff re-audit returned no findings and confirmed both P1/P2 corrections. A live debugger-detached controlled report was intentionally not sent and remains an opt-in operational smoke check, not a 02.6 configuration blocker.
- Subphase 02.5 RED: `LocalizationConfigurationTests/criticalLocalizationSymbolIsGenerated()` referenced `.bootstrapWelcomeTitle` before a catalog existed, and Xcode MCP rejected the test build with `Type 'LocalizedStringResource' has no member 'bootstrapWelcomeTitle'`. GREEN: `FranAlonso/Resources/Localizable.xcstrings` now owns the manual semantic key `bootstrap.welcome.title`, its Spanish source value, and translator context; `ContentView` consumes `LocalizedStringResource.bootstrapWelcomeTitle`, and the test asserts the generated resource's underlying key. The recommended-settings update had already enabled `STRING_CATALOG_GENERATE_SYMBOLS = YES` for the project and app target in Debug/Release, so no redundant flag change was needed. The final Xcode MCP build succeeds in 3.143 seconds with 0 build-log warnings and 0 warning-level Navigator issues; the full Swift Testing plan passes 3/3. The build log records `xcstringstool generate-symbols`, the derived source contains `static var bootstrapWelcomeTitle`, the product contains `es.lproj/Localizable.strings`, and its Info.plist declares `CFBundleDevelopmentRegion = es`. Normal and AX 5 Dynamic Type previews render `Bienvenido a Fran Alonso` without clipping. Both independent read-only audits returned no findings and confirmed the generated source/index is the basis for Xcode code completion. Their residual notes were that Release was inspected rather than compiled separately, VoiceOver was not run manually, and the isolated `XcodeRefreshCodeIssuesInFile` action does not load the target's generated `DerivedSources`; the full compiler build and user-visible Issue Navigator remain clean, so none blocks 02.5.
- Subphase 02.4 required no executable or project-setting change: the repository already contained only the application target and `FranAlonsoTests`, with `import Testing`, `@Suite`, `@Test`, and `#expect`; no XCTest/XCUITest import, UI-test API, source, target, scheme entry, or test-plan entry was found. A new RED is `N/A` because there was no behavior to implement and the development guide forbids artificial tests for verification-only work; the existing concurrency case supplies the required minimal `@Test`/`#expect` acceptance fixture. Xcode MCP lists exactly 2 enabled tests in the `FranAlonso` plan, the full plan passes 2/2, the app build succeeds in 3.223 seconds, and both the warning-level build log and Issue Navigator contain 0 entries. The `.xctest` product suffix is the normal unit-test bundle and does not imply an XCTest import. No ADR change is required because the final state implements accepted ADR 0003 without an exception. Both independent read-only audits returned no findings; they confirmed target membership, active scheme/test-plan coverage, the `N/A` justification, and the clean full diff. Their only residual note was that Release was inspected rather than compiled separately, which is not a 02.4 requirement or blocker.
- Subphase 02.3 first RED: the Swift 6 compile guard in `ProjectSanityTests.swift` stopped the test build because `FranAlonsoTests` still used Swift 5 (`FranAlonsoTests must compile in Swift 6 language mode`). Second RED: after aligning settings, a temporary isolation violation was rejected because a `@MainActor` property could not be mutated from a nonisolated context. GREEN: the violation was removed; a permanent Swift Testing case now verifies that default isolation remains `nonisolated`, the full plan passes 2/2, and Xcode MCP builds successfully in 6.737 seconds with 0 build-log warnings and 0 warning-level Navigator issues. App and tests declare Swift 6, complete strict concurrency, and `nonisolated` default isolation in every Debug/Release target configuration; no unsafe concurrency escape hatch was introduced. Both independent read-only audits returned no findings. Their only residual notes were that the executable fixture directly exercises the test module while the app setting is covered by configuration inspection and build, and that Release was inspected rather than compiled separately; neither is a 02.3 requirement or blocker.
- Subphase 02.2 RED: with `SWIFT_TREAT_WARNINGS_AS_ERRORS = YES` and `GCC_TREAT_WARNINGS_AS_ERRORS = YES` in both project configurations, a temporary deprecated-call fixture in `AppRoot.swift` made the Xcode MCP build fail in 8.102 seconds for the expected reason (`controlledWarningFixture()` is deprecated). GREEN: after removing the fixture completely, Xcode MCP built successfully in 5.925 seconds; the full Swift Testing plan passed 1/1, the warning-level build log contained 0 entries, the Issue Navigator contained 0 warnings, and `git diff --check` was clean. The compiler log confirms `-warnings-as-errors` for both app and test Swift compilation; Clang warnings-as-errors is configured by the inherited project-level setting. No artificial test or warning fixture remains. Both independent read-only audits returned no findings; their only residual note was that reproducing the historical RED would require mutating the project, so they verified the recorded diagnosis and the clean final state instead.
- Subphase 02.1 RED: Xcode MCP could not compile `ProjectSanityTests/compositionRootCanBeCreated()` because `AppRoot` did not exist (`Cannot find 'AppRoot' in scope`). GREEN: after adding the minimal SwiftUI `AppRoot` under `App/` and connecting `FranAlonsoApp`, the targeted test passed 1/1. Final implementer validation after the structural cleanup: Xcode MCP app build succeeded in 6.361 seconds with 0 warnings, 0 errors, and 0 Navigator issues; `git diff --check` is clean. The deployment target is iOS 26.0 in every project, app, and test configuration. Runtime smoke validation through the Xcode interface succeeded on iPhone 17 and iPad (A16), both using iOS 26.5 and displaying the expected initial `ContentView`. The official 21-tool Xcode MCP surface used for this project exposes build and test actions but no Run action, so the 02.1 validation wording records that executable launch fallback explicitly. The first independent audit raised only the missing runtime evidence; after the two launches, the same reviewer confirmed the finding resolved with no residual risks, and the second independent full-diff audit returned no findings or residual risks in the 01.9/02.1 scope.
- Phase 01 acceptance: the product owner reviewed and accepted ADR 0001–0009 on 2026-07-14. This subphase is documentation-only, so build/tests are `N/A`. Implementer validation: `git diff --check` clean; 37 Markdown files checked with 0 broken local links; 9 ADR files and 9 index rows checked with 0 status-consistency failures. The first independent audit produced two documentation-state findings and both were incorporated; the second full-diff audit confirmed both resolutions and returned no findings or residual risks in the 01.9 scope.
- Documentation-only design pass: `docs/design/brand-palettes.md` records the selected palette and retained alternatives; `docs/design/navigation.md` records the adaptive shell and the immediate Jornada-to-Histórico closure rule. No executable target changed, so build/test evidence is `N/A`. Validation: `git diff --check` clean; 37 Markdown files checked with 0 broken local links; 60 palette contrast pairs checked with 0 failures. Two independent `$review-ios-standards` audits completed; all findings were incorporated, including terminal `voided` operations remaining visible and differentiated in Histórico.
- Icon Composer RED: Xcode MCP failed because `ASSETCATALOG_COMPILER_APPICON_NAME` passed `franalonso.icon` as the logical `--app-icon` name.
- Icon Composer GREEN: the `.icon` document remained an asset-catalog input, Debug and Release now use the extensionless logical name `franalonso`, and Xcode MCP completed a clean app build with no errors.
- TDD evidence for this configuration repair: the failing build was the RED check and the successful rebuild was the GREEN check; no artificial Swift test was added for an asset-compiler setting.
- The former empty `FranAlonsoTests/example()` template has been replaced by `ProjectSanityTests/applicationCompositionRootCanBeCreated()`.
- Project management is connected through the [FranAlonso Linear project](https://linear.app/plusprojects/project/franalonso-ced946bf6e7e) and an Obsidian project dashboard; this repository remains the source of truth.
- Review evidence for the earlier Firebase/icon repair: two isolated read-only reviewer attempts did not return a result, so the documented fallback review corrected stale UITest-removal and ATT wording; a second full-diff read-only pass found no remaining issue in that repair scope. The recommended-settings warning has since been resolved and validated during 02.1.

## Immediate Next Actions

1. Prepare the separate source-backed phase 07 proposal; do not start executable design/navigation work before its independent review and explicit owner approval.
2. Keep all four composed engines inactive and retain the closed Sales namespace until a separately approved live gate proves writers, Rules, contention, indexes, size and rollback.
3. Before any later controlled live Clients access, inspect the manually created `develop` document identity and Codable shape without overwriting it, then approve a dedicated trigger and smoke-test scope. Plan the legacy application's Authentication/App Check migration before replacing the temporary `prod/prod`, `dev/dev` and `backup/backup` grants with collection-level minimum privilege in phases 06/17.

## Blockers

- The global Firestore exposure is closed, but `prod/prod`, `dev/dev` and `backup/backup` intentionally remain anonymously readable and writable for the unauthenticated legacy application. This temporary compatibility is not the final security posture; it must not be broadened and can only be removed after the incumbent app migrates to identity and abuse protection.
- The manually created `develop` client must have its document identity and Codable schema inspected before the first controlled live Clients read or overwrite. Legal approval of the consent text and an original wordmark remain production prerequisites, not Data-layer implementation blockers. Phase 16 additionally requires a compatible Apple Intelligence device/language, final nearby-voice disclosure and physical-device evidence.
